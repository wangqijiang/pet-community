# WaggleWorld 宠物社交小程序 PRD 文档（完整版·可下载MD）

# 一、项目概述

## 1\.1 项目背景

随着国内养宠人群持续扩大，宠物主群体呈现年轻化、精致化趋势，核心痛点集中在三点：一是**同城同品种宠物主社交难**，缺乏便捷的线下遛狗、宠物互动渠道；二是**科学养宠知识获取碎片化**，新手主人难以快速获取适配自家宠物的饲养建议；三是**线下遛狗场景资源分散**，无法快速找到宠物友好地点、门店。WaggleWorld 定位为轻量型宠物社交工具小程序，以“软萌治愈”为视觉核心，聚焦“同城社交、科学养宠、线下场景”三大需求，打造一站式宠物主服务平台，兼顾易用性与趣味性，避免冗余功能，提升用户留存与活跃度。

## 1\.2 产品定位

- **目标用户**：18\-35岁养宠人群（以犬类、猫类主人为主），偏好治愈系视觉设计，注重宠物社交、科学饲养，追求轻量化产品体验，使用场景多为碎片化时间（通勤、遛狗间隙、休息时）。

- **核心Slogan**：懂你宠物的每一刻情绪

- **产品定位**：轻量化、高易用性、软萌治愈的宠物社交工具型小程序，连接同城宠物主、提供科学养宠辅助、整合线下遛狗资源。

- **核心价值**：让宠物主快速找到同好、获取专属养宠攻略、发现附近遛狗好去处，打造轻松愉悦的宠物社交体验。

## 1\.3 产品核心功能模块（四大模块，全闭环）

1. 首页模块：同城狗友地图（核心场景：找同城同品种宠物主、找遛狗地点）

2. 萌宠圈模块：动态社区（核心场景：分享宠物日常、查看他人动态、互动交流）

3. 消息模块：私信与系统通知（核心场景：与同城宠物主沟通、接收系统提醒）

4. 我的模块：个人中心\+AI养宠助手（核心场景：管理个人/宠物信息、获取科学养宠攻略）

## 1\.4 产品版本与迭代规划（V1\.0 完整版）

- **版本目标**：完成四大核心模块所有功能闭环，实现“找同好、找遛弯地、科学养宠、社交互动”全部核心场景，确保UI风格统一、交互流畅、逻辑无漏洞，适配微信小程序规范，可直接上线使用。

- **迭代优先级**：核心功能（地图筛选、宠物管理、动态发布、私信）→ 辅助功能（AI养宠助手、攻略保存）→ 优化功能（交互细节、视觉优化）

- **暂不实现功能**：付费功能、多端互通（仅微信小程序）、复杂社区运营（如话题、排行榜）、宠物医疗问诊、宠物交易等延伸功能。

# 二、产品整体规范（全局统一，所有页面必须遵守）

## 2\.1 视觉风格规范（核心：软萌治愈，无尖角、高圆润度）

### 2\.1\.1 配色规范（全局统一，无例外）

- 主色调：奶白色（\#FFF9F9）（页面底色、卡片底色）

- 主功能色：淡粉色（\#FFC1E9）（按钮、选中状态、图标、标题）

- 辅助色：浅奶黄（\#FFF4D2）（标签、部分按钮）、柔浅蓝（\#E0F7FF）（副标题、链接文字）

- 中性色：浅灰色（\#E5E5E5）（未选中图标、边框、次要文字）、深灰色（\#333333）（核心内容文字）

### 2\.1\.2 UI元素规范

- **圆角**：所有元素（卡片、按钮、输入框、弹窗、头像、图标）均采用大圆角设计，圆角尺寸统一为12px（特殊组件如圆形头像、圆形按钮除外，采用全圆角），无任何尖锐边角。

- **字体**：全局使用圆润可爱无衬线字体（如“快乐体”“站酷快乐体”），标题字体加粗、字号较大，正文字体适中，辅助文字字号较小，行间距统一为1\.5倍，确保可读性。

- **图标**：所有图标（返回、消息、设置、点赞、评论等）均为软萌卡通风格，颜色与全局配色一致，尺寸统一，避免尖锐线条。

### 2\.1\.3 布局规范

- 所有页面采用“顶部导航栏\+主体内容\+底部Tab栏”的固定布局（除弹窗、详情页等特殊页面外）。

- 主体内容间距统一：上下间距20px，左右间距16px，卡片之间间距12px，确保页面整洁、不拥挤。

- 所有可点击元素（按钮、卡片、图标、文字链接）均需添加明显的可点击标识（如箭头、高亮颜色、粒子特效），避免用户误触或找不到交互入口。

## 2\.2 交互动效规范（全局统一，所有可点击元素必须遵守）

### 2\.2\.1 基础交互特效（所有可点击元素通用）

- 按压特效：点击时元素轻微缩小至0\.98倍，松开后回弹至原尺寸，伴随轻微手机震动（震动强度适中，不突兀）。

- 粒子特效：点击后飘散3\-5个淡粉色小爱心粒子，粒子大小不一，缓慢上升后消失，特效持续0\.5秒，不遮挡核心内容。

### 2\.2\.2 页面切换特效

- 页面跳转：采用“柔和滑动\+淡入淡出”过渡，过渡时长0\.3秒，避免生硬切换。

- 页面返回：与跳转特效一致，保持交互统一性。

### 2\.2\.3 弹窗特效

- 弹出：轻微从小到大弹跳淡入（弹跳幅度适中），过渡时长0\.2秒。

- 关闭：平滑淡入淡出消失，过渡时长0\.2秒，点击空白区域可触发关闭（加载弹窗除外）。

### 2\.2\.4 加载特效

- 全局统一使用“小狗转圈”可爱加载动画，动画颜色为淡粉色，大小适中，位于页面居中位置。

- 加载文案统一为“加载中…”，字体圆润，颜色为淡粉色，位于动画下方。

## 2\.3 全局通用组件规范（独立组件，可复用，所有页面统一套用）

### 2\.3\.1 顶部导航栏组件（全页面复用，无例外）

#### 2\.3\.1\.1 基础样式

- 尺寸：宽度全屏（适配所有手机屏幕），高度固定为48px（包含安全边距）。

- 背景：纯白色（\#FFF9F9），底部添加1px淡粉色（\#FFC1E9）细边框，边框不突兀。

- 布局：采用“左\-中\-右”三栏布局，左右两侧留16px安全边距，中间标题居中对齐。

#### 2\.3\.1\.2 左侧区域（固定，所有页面均有）

- 元素：可爱小狗爪印形状的返回图标（颜色为淡粉色\#FFC1E9），图标尺寸为24px×24px。

- 交互逻辑：
                  点击返回图标，触发页面返回上一页操作，同时触发全局按压特效（微缩回弹\+震动\+粒子）。

- 若当前页面为一级页面（首页、萌宠圈、消息、我的），点击返回图标无操作（避免退出小程序）。

#### 2\.3\.1\.3 中间区域（可动态修改，随页面变化）

- 元素：页面标题文字，字体为圆润加粗字体，字号18px，颜色为淡粉色（\#FFC1E9），居中显示。

- 规则：标题字数不超过8个字，超出部分显示省略号（…），确保布局美观。

#### 2\.3\.1\.4 右侧区域（可动态添加，随页面功能变化）

- 元素：可根据页面功能添加对应图标（如消息铃铛、设置齿轮、刷新、添加\+、搜索等），所有图标尺寸统一为24px×24px，颜色为淡粉色（\#FFC1E9）。

- 交互逻辑：点击右侧图标，触发对应功能操作，同时触发全局按压特效；若右侧无功能需求，该区域为空（不占位）。

#### 2\.3\.1\.5 补充规则

- 导航栏固定在页面顶部，不随页面滚动（始终可见）。

- 所有页面的导航栏结构、样式、尺寸完全统一，仅中间标题和右侧图标可根据页面需求修改。

### 2\.3\.2 底部Tab栏组件（全页面复用，无例外）

#### 2\.3\.2\.1 基础样式

- 尺寸：宽度全屏（适配所有手机屏幕），高度固定为56px（包含底部安全边距，适配全面屏手机）。

- 背景：纯白色（\#FFF9F9），顶部添加1px淡粉色（\#FFC1E9）细边框，边框不突兀。

- 布局：采用均匀分布布局，4个Tab项平均占用全屏宽度，无额外间距。

#### 2\.3\.2\.2 Tab项规范（固定4个，不可修改，顺序不可调整）

|Tab名称|图标样式（未选中/选中）|文字样式（未选中/选中）|对应页面|
|---|---|---|---|
|首页|未选中：浅灰色（\#E5E5E5）爪印图标；选中：淡粉色（\#FFC1E9）爪印图标，轻微放大至1\.1倍|未选中：浅灰色（\#E5E5E5），字号12px；选中：淡粉色（\#FFC1E9），字号12px，加粗|同城狗友地图页|
|萌宠圈|未选中：浅灰色（\#E5E5E5）朋友圈图标；选中：淡粉色（\#FFC1E9）朋友圈图标，轻微放大至1\.1倍|未选中：浅灰色（\#E5E5E5），字号12px；选中：淡粉色（\#FFC1E9），字号12px，加粗|萌宠圈主页面|
|消息|未选中：浅灰色（\#E5E5E5）铃铛图标；选中：淡粉色（\#FFC1E9）铃铛图标，轻微放大至1\.1倍；有未读消息时，图标右上角添加红色小圆点（直径8px）|未选中：浅灰色（\#E5E5E5），字号12px；选中：淡粉色（\#FFC1E9），字号12px，加粗|消息主页面|
|我的|未选中：浅灰色（\#E5E5E5）用户头像图标；选中：淡粉色（\#FFC1E9）用户头像图标，轻微放大至1\.1倍|未选中：浅灰色（\#E5E5E5），字号12px；选中：淡粉色（\#FFC1E9），字号12px，加粗|个人中心主页面|

#### 2\.3\.2\.3 交互逻辑

- 点击Tab项：触发页面切换操作，同时触发全局按压特效（微缩回弹\+震动\+粒子），选中的Tab项立即切换为选中状态，其他Tab项切换为未选中状态。

- 重复点击当前选中的Tab项：无任何操作（避免重复跳转，提升用户体验）。

- 页面切换时：Tab栏始终固定在页面底部，不随页面切换消失，保持视觉统一性。

#### 2\.3\.2\.4 补充规则

- 所有页面底部均需显示该Tab栏，无例外（弹窗、全屏预览等特殊场景除外）。

- Tab项的图标、文字、颜色、尺寸完全固定，不可修改。

### 2\.3\.3 全局弹窗组件（全页面复用，所有弹窗统一套用）

#### 2\.3\.3\.1 基础样式（所有弹窗通用）

- 尺寸：宽度固定为屏幕的85%，高度自适应内容（最小高度120px，最大高度不超过屏幕的60%）。

- 背景：纯白色（\#FFF9F9），添加1px淡粉色（\#FFC1E9）细边框，圆角为12px（大圆角）。

- 内边距：弹窗内部上下内边距20px，左右内边距16px，确保内容不拥挤。

- 阴影：弹窗添加轻微阴影（rgba\(255, 193, 233, 0\.3\)），增强层次感，不突兀。

#### 2\.3\.3\.2 弹窗类型（四种标准类型，覆盖全程序所有弹窗场景，无额外弹窗样式）

##### 类型1：提示弹窗（用于成功、失败、提醒等场景，如保存成功、昵称不能为空等）

- **结构**：顶部图标 \+ 标题文字 \+ 内容文字 \+ 底部单个按钮

- **详细样式**：
                  顶部图标：尺寸32px×32px，根据场景选择对应图标（成功：淡粉色对勾图标；失败：淡粉色叉号图标；提醒：淡粉色感叹号图标；普通提示：淡粉色小狗图标）。

- 标题文字：居中显示，圆润加粗字体，字号16px，颜色淡粉色（\#FFC1E9），位于图标下方，间距8px。

- 内容文字：居中显示，圆润字体，字号14px，颜色深灰色（\#333333），位于标题下方，间距8px，行间距1\.5倍，可换行。

- 底部按钮：通栏大圆角按钮（圆角12px），高度44px，背景淡粉色（\#FFC1E9），文字为白色、圆润加粗字体、字号16px，按钮与内容文字间距16px。

- **交互逻辑**：
                  点击底部按钮：触发关闭弹窗操作，同时触发全局按压特效，弹窗淡入淡出消失。

- 点击弹窗空白区域：触发关闭弹窗操作，弹窗淡入淡出消失。

- 弹窗弹出后，无自动关闭功能，需用户手动点击关闭。

##### 类型2：确认弹窗（用于需要二次确认的操作，如删除动态、取消关注、退出登录等）

- **结构**：顶部图标 \+ 标题文字 \+ 内容文字 \+ 底部双按钮

- **详细样式**：
                  顶部图标：尺寸32px×32px，淡粉色疑问图标（或对应场景图标，如删除用淡粉色垃圾桶图标）。

- 标题文字：居中显示，圆润加粗字体，字号16px，颜色淡粉色（\#FFC1E9），位于图标下方，间距8px。

- 内容文字：居中显示，圆润字体，字号14px，颜色深灰色（\#333333），位于标题下方，间距8px，行间距1\.5倍，可换行（内容为确认操作的提示，如“确定要删除这条动态吗？”）。

- 底部双按钮：左右等分排列，每个按钮宽度为弹窗宽度的45%，高度44px，圆角12px；左侧按钮：背景浅灰色（\#E5E5E5），文字深灰色（\#333333）、圆润字体、字号16px；右侧按钮：背景淡粉色（\#FFC1E9），文字白色、圆润加粗字体、字号16px；两个按钮间距8px，与内容文字间距16px。

- **交互逻辑**：
                  点击左侧“取消”按钮：触发关闭弹窗操作，不执行任何核心操作，弹窗淡入淡出消失，同时触发全局按压特效。

- 点击右侧“确定”按钮：执行对应核心操作（如删除动态、取消关注），操作完成后，弹窗淡入淡出消失，同时触发全局按压特效，页面同步刷新（如删除动态后，列表实时刷新）。

- 点击弹窗空白区域：触发关闭弹窗操作，不执行任何核心操作，弹窗淡入淡出消失。

##### 类型3：选择弹窗（用于需要用户选择的场景，如更换头像时选择“拍照/从相册选择”、选择宠物等）

- **结构**：顶部标题 \+ 中间选择列表 \+ 底部单个关闭按钮

- **详细样式**：
                  顶部标题：居中显示，圆润加粗字体，字号16px，颜色淡粉色（\#FFC1E9），无图标。

- 中间选择列表：位于标题下方，间距16px；每个选择项为大圆角矩形（圆角12px），高度44px，背景奶白色（\#FFF9F9），文字居中显示、圆润字体、字号14px、颜色深灰色（\#333333）；选择项之间间距8px；选中的选择项，背景变为淡粉色（\#FFC1E9），文字变为白色。

- 底部关闭按钮：通栏大圆角按钮（圆角12px），高度44px，背景浅灰色（\#E5E5E5），文字深灰色（\#333333）、圆润字体、字号16px；按钮与选择列表间距16px。

- **交互逻辑**：
                  点击选择列表中的任意选项：选中该选项（切换选中状态），同时触发全局按压特效；若为“单选”场景，选中一个选项后，其他选项自动取消选中。

- 选中选项后，可点击该选项执行对应操作（如选择“拍照”，立即唤起相机），操作完成后，弹窗自动关闭。

- 点击底部“关闭”按钮：触发关闭弹窗操作，不执行任何选择操作，弹窗淡入淡出消失，同时触发全局按压特效。

- 点击弹窗空白区域：触发关闭弹窗操作，不执行任何选择操作，弹窗淡入淡出消失。

##### 类型4：加载弹窗（用于需要等待的操作，如保存资料、生成AI攻略、发布动态等）

- **结构**：中间加载动画 \+ 下方加载文案

- **详细样式**：加载动画：居中显示，尺寸40px×40px，全局统一为“小狗转圈”可爱动画，颜色淡粉色（\#FFC1E9），动画循环播放。

- 加载文案：居中显示，位于动画下方，间距8px，圆润字体，字号14px，颜色淡粉色（\#FFC1E9），文案固定为“加载中…”，不可修改。

- **交互逻辑**：
                  加载弹窗弹出后，不可点击空白区域关闭，不可点击任何按钮，仅当加载完成后，自动淡入淡出消失。

- 加载完成后，根据操作结果，自动弹出对应提示弹窗（如保存成功提示、生成失败提示）。

#### 2\.3\.3\.3 补充规则

- 同一时间，页面仅可弹出一个弹窗，不可叠加弹出多个弹窗。

- 所有弹窗的样式、动画、交互逻辑完全统一，仅内容、图标可根据场景修改。

- 弹窗弹出时，背景页面添加轻微半透明遮罩（rgba\(255, 255, 255, 0\.7\)），不遮挡背景页面，但突出弹窗，提升用户注意力。

### 2\.3\.4 其他通用组件（辅助组件，全页面复用）

#### 2\.3\.4\.1 输入框组件

- 样式：大圆角（12px），高度44px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距左右16px。

- 文字：输入文字为深灰色（\#333333），圆润字体，字号14px；占位文字为浅灰色（\#E5E5E5），圆润字体，字号14px。

- 交互：获取焦点时，边框变为粗版淡粉色（\#FFC1E9）；失去焦点时，边框恢复细版淡粉色；输入错误时，边框变为淡红色，下方显示浅红色错误提示文字。

#### 2\.3\.4\.2 按钮组件（通用按钮，非弹窗按钮）

- 样式：大圆角（12px），高度44px，分为两种类型：
                  主按钮：背景淡粉色（\#FFC1E9），文字白色、圆润加粗字体、字号16px。

- 次要按钮：背景浅灰色（\#E5E5E5），文字深灰色（\#333333）、圆润字体、字号16px。

- 交互：点击时触发全局按压特效（微缩回弹\+震动\+粒子）；按钮置灰时（如输入为空时），不可点击，无任何交互特效。

#### 2\.3\.4\.3 卡片组件（通用卡片，用于列表、入口等）

- 样式：大圆角（12px），背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距16px；卡片阴影轻微（rgba\(255, 193, 233, 0\.2\)）。

- 交互：可点击的卡片，点击时触发全局按压特效；不可点击的卡片，无任何交互效果，仅作展示。

# 三、功能模块详细说明（四大模块，含所有子页面、交互逻辑、细节说明，无省略）

## 3\.1 首页模块：同城狗友地图（核心页面，页面路径：/pages/home/index）

### 3\.1\.1 页面结构（从上到下，完整无省略）

1. 顶部导航栏组件（复用全局组件）
                  左侧：返回图标（无操作，因首页为一级页面）

2. 中间：标题“同城狗友地图”

3. 右侧：铃铛消息图标（点击跳转消息主页面）

4. 狗狗品种筛选栏（单条可折叠，核心筛选组件）

5. 全屏地图主区域（页面核心内容，占比最大）
                  地图背景：淡蓝色（\#E0F7FF）柔和底色，叠加极低透明度（10%）的淡灰色卡通小狗脚印暗纹，不抢主视觉。

6. 三种点位：用户点位、宠物友好地点点位、宠物门店点位。

7. 地图右上角：悬浮定位按钮（圆形，直径40px，淡粉色背景，白色靶心图标）。

8. 底部快捷功能卡片（两个并排卡片，固定在地图下方，不随地图滚动）
左侧卡片：同片区热门狗友

9. 右侧卡片：附近遛狗好去处

10. 底部Tab栏组件（复用全局组件）

### 3\.1\.2 各区域详细样式与交互逻辑（无省略）

#### 3\.1\.2\.1 狗狗品种筛选栏（核心筛选功能，仅筛选狗狗品种，无其他筛选项）

##### 3\.1\.2\.1\.1 未展开状态（默认状态）

- 样式：横向单条大圆角（12px）卡片，高度44px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，宽度为屏幕宽度的90%，居中显示。

- 内部布局：左\-中\-右三栏，左右间距16px。
                  左侧：淡粉色（\#FFC1E9）大圆角（8px）标签，文字“狗狗品种”，圆润字体，字号14px，白色文字， padding左右8px、上下4px。

- 中间：当前选中状态文字，圆润字体，字号14px，深灰色（\#333333），默认显示“全部品种”，选中具体品种后，显示品种名称（如“金毛”“柯基”）。

- 右侧：淡粉色（\#FFC1E9）下拉箭头图标，尺寸20px×20px，可点击。

- 交互逻辑：点击筛选栏任意位置（左侧标签、中间文字、右侧箭头），均可触发筛选面板展开操作，同时触发全局按压特效；展开时，筛选栏边框变为粗版淡粉色（\#FFC1E9），下拉箭头旋转180°。

##### 3\.1\.2\.1\.2 展开状态（点击筛选栏后）

- 样式：从筛选栏下方滑出的大圆角（12px）半透明面板，背景奶白色（\#FFF9F9，透明度95%），淡粉色（\#FFC1E9）细边框，宽度与筛选栏一致（屏幕宽度90%），高度自适应内容（最大高度不超过屏幕的40%），面板与筛选栏无缝衔接，无间距。

- 内部布局（从上到下）：
                  一键清除按钮：大圆角（8px）按钮，背景淡粉色（\#FFC1E9），文字白色、圆润加粗字体、字号14px，padding左右12px、上下8px，位于面板顶部左侧。

- 热门品种标签条：横向可滑动，位于一键清除按钮下方，间距12px；每个标签为大圆角（8px），高度32px，未选中时背景奶白色、淡粉色文字，选中时背景淡粉色、白色文字，标签内包含对应品种的可爱小头像（尺寸20px×20px）\+ 品种名称（如“金毛”“柯基”），标签之间间距8px；热门品种固定为：金毛、柯基、泰迪、柴犬、哈士奇、萨摩耶（6种）。

- 搜索框：复用全局输入框组件，位于热门品种标签条下方，间距12px，占位文字“搜索品种名称”。

- 全部品种列表：位于搜索框下方，间距12px；列表按首字母分组（如A、B、C…），每组首字母为淡粉色加粗字体，字号14px；每个品种条目为大圆角（8px），高度36px，背景奶白色，文字圆润字体、字号14px、深灰色，左侧添加对应品种小头像（尺寸16px×16px），条目之间间距4px；列表支持垂直滚动，滚动时搜索框固定在顶部。

- 确定按钮：复用全局主按钮组件，通栏，位于面板底部，间距12px，文字“确定”。

- 交互逻辑：
                  一键清除按钮：点击触发“清除筛选”操作，同时触发全局按压特效；清除后，中间选中状态文字恢复为“全部品种”，筛选面板自动收起，地图点位实时刷新为所有品种。

- 热门品种标签：点击任意标签，触发“选中品种”操作，同时触发全局按压特效；选中后，标签切换为选中状态，中间选中状态文字同步更新为该品种名称；可点击多个热门品种标签（支持多选），选中多个品种后，地图点位显示所有选中品种的用户。

- 搜索框：输入文字时，实时过滤全部品种列表，仅显示包含搜索关键词的品种；输入为空时，列表恢复显示所有品种；支持中文、拼音搜索（如输入“jinmao”可搜索到“金毛”）。

- 全部品种列表：点击任意品种条目，触发“选中品种”操作，同时触发全局按压特效；选中后，条目背景变为淡粉色，文字变为白色，中间选中状态文字同步更新为该品种名称；可选中多个品种（支持多选），选中多个品种后，地图点位显示所有选中品种的用户；点击已选中的品种条目，取消选中，中间选中状态文字同步更新（若取消所有选中，恢复为“全部品种”）。

- 确定按钮：点击触发“确认筛选”操作，同时触发全局按压特效；确认后，筛选面板自动收起，地图点位实时刷新为选中的品种（若未选中任何品种，刷新为所有品种）。

- 面板收起：除了点击确定按钮、一键清除按钮，点击面板空白区域、筛选栏，均可触发面板收起操作，面板淡入淡出消失，筛选栏边框恢复细版淡粉色，下拉箭头旋转回原位。

#### 3\.1\.2\.2 全屏地图主区域（核心展示区域）

##### 3\.1\.2\.2\.1 地图基础交互

- 支持双指缩放（放大/缩小地图）、单指拖动（移动地图视角）。

- 支持下拉刷新：下拉地图顶部，触发刷新操作，显示小狗转圈加载动画，刷新完成后，地图点位重新加载（同步最新的用户、地点、门店数据）。

- 地图默认视角：显示用户当前所在片区（模糊定位，不显示精确坐标），地图缩放比例适中，确保能看到周边1\-3km内的点位。

##### 3\.1\.2\.2\.2 地图右上角悬浮定位按钮

- 样式：圆形（直径40px），淡粉色（\#FFC1E9）背景，白色靶心图标（尺寸20px×20px），轻微阴影，固定在地图右上角，距离顶部导航栏下方20px、右侧16px。

- 交互逻辑：点击按钮，触发“回到当前片区”操作，同时触发全局按压特效；点击后，地图自动缩放并定位到用户当前所在片区，显示定位动画（淡粉色圆圈扩散效果）。

##### 3\.1\.2\.2\.3 三种点位详细说明（样式\+交互，无省略）

###### 点位1：用户点位（展示同城公开位置的养狗用户）

- 样式：圆形宠物头像（直径36px），带淡粉色（\#FFC1E9）细边框（1px），头像下方添加大圆角（8px）标签，背景淡粉色，文字为宠物名称（如“糯米”），白色圆润字体，字号12px，标签长度自适应文字。

- 交互逻辑：
                  点击点位：弹出用户信息弹窗（复用全局提示弹窗样式，宽度为屏幕70%），弹窗内包含：
                          顶部：宠物头像（圆形，直径48px）\+ 用户昵称（圆润加粗字体，淡粉色，字号14px）

- 中间：宠物品种、体型标签（大圆角8px，浅奶黄背景，柔浅蓝文字，字号12px，两个标签并排，间距4px）

- 底部：两个并排按钮（左：“查看主页”，右：“发私信”），按钮样式复用全局按钮组件（主按钮）。

11. 弹窗交互：
                          点击“查看主页”：关闭弹窗，平滑跳转至该用户的他人用户主页详情页，同时触发全局按压特效。

12. 点击“发私信”：关闭弹窗，平滑跳转至与该用户的私信聊天页面，同时触发全局按压特效；若为陌生人，跳转后自动弹出提示弹窗（“陌生人仅可发送1条私信，对方回复后可正常聊天”）。

13. 点击弹窗空白区域：关闭弹窗，淡入淡出消失。

14. 点位显示规则：仅显示当前筛选品种的用户点位；若用户关闭“隐私可见模式”，其点位不显示在地图上。

###### 点位2：宠物友好地点点位（展示适合遛狗的公园、草坪等）

- 样式：浅粉色（\#FFC1E9）地标图标（尺寸32px×32px），图标下方添加大圆角（8px）标签，背景淡粉色，文字为地点名称（如“宠物草坪公园”），白色圆润字体，字号12px，标签长度自适应文字。

- 交互逻辑：
                  点击点位：弹出地点信息弹窗（复用全局提示弹窗样式，宽度为屏幕70%），弹窗内包含：
                          顶部：地点封面图（大圆角12px，尺寸16:9，宽度全屏）

- 中间：地点名称（圆润加粗字体，淡粉色，字号14px）、地点简介（圆润字体，深灰色，字号12px，可换行）

- 底部：单个“立即导航”按钮（复用全局主按钮组件）。

15. 弹窗交互：
                          点击“立即导航”：关闭弹窗，唤起微信地图，自动输入该地点地址，生成导航路线，同时触发全局按压特效。

16. 点击弹窗空白区域：关闭弹窗，淡入淡出消失。

###### 点位3：宠物门店点位（展示宠物医院、宠物店、宠物美容店等）

- 样式：浅粉色（\#FFC1E9）小屋图标（尺寸32px×32px），图标下方添加大圆角（8px）标签，背景淡粉色，文字为门店名称（如“汪汪宠物店”），白色圆润字体，字号12px，标签长度自适应文字。

- 交互逻辑：
                  点击点位：弹出门店信息弹窗（复用全局提示弹窗样式，宽度为屏幕70%），弹窗内包含：
                          顶部：门店封面图（大圆角12px，尺寸16:9，宽度全屏）

- 中间：门店名称（圆润加粗字体，淡粉色，字号14px）、门店分类标签（大圆角8px，浅奶黄背景，柔浅蓝文字，字号12px）、详细地址（圆润字体，深灰色，字号12px）、营业时间（圆润字体，深灰色，字号12px）、特色简介（圆润字体，深灰色，字号12px，可换行）

- 底部：两个并排按钮（左：“查看详情”，右：“立即导航”），按钮样式复用全局按钮组件（主按钮）。

17. 弹窗交互：
                          点击“查看详情”：关闭弹窗，平滑跳转至门店详情页，同时触发全局按压特效。

18. 点击“立即导航”：关闭弹窗，唤起微信地图，自动输入该门店地址，生成导航路线，同时触发全局按压特效。

19. 点击弹窗空白区域：关闭弹窗，淡入淡出消失。

#### 3\.1\.2\.3 底部快捷功能卡片（两个并排，固定在地图下方）

##### 3\.1\.2\.3\.1 左侧卡片：同片区热门狗友

- 样式：大圆角（12px）卡片，宽度为屏幕45%，高度100px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距16px；卡片内部布局：顶部可爱小狗群居图标（淡粉色，尺寸24px×24px），中间标题“同片区热门狗友”（圆润加粗字体，淡粉色，字号16px），底部副标题“查看附近的毛孩子家长”（圆润字体，浅灰色，字号12px）；卡片轻微阴影，提升层次感。

- 交互逻辑：点击整张卡片，触发跳转操作，同时触发全局按压特效；关闭当前页面，平滑跳转至同片区热门狗友列表页，跳转后页面自动刷新，显示当前片区的养狗用户列表。

##### 3\.1\.2\.3\.2 右侧卡片：附近遛狗好去处

- 样式：与左侧卡片完全一致（尺寸、圆角、背景、边框、阴影均相同）；卡片内部布局：顶部草坪公园图标（淡粉色，尺寸24px×24px），中间标题“附近遛狗好去处”（圆润加粗字体，淡粉色，字号16px），底部副标题“公园/草坪/友好商圈导航”（圆润字体，浅灰色，字号12px）。

- 交互逻辑：点击整张卡片，触发跳转操作，同时触发全局按压特效；关闭当前页面，平滑跳转至附近遛狗好去处列表页，跳转后默认显示“宠物友好地点”分类，页面自动刷新。

### 3\.1\.3 空状态与加载状态（无省略）

#### 3\.1\.3\.1 地图空状态（三种场景）

- 场景1：无任何用户点位（当前片区无公开位置的养狗用户）：地图居中显示可爱小狗发呆插画（尺寸120px×120px），插画下方显示文字“暂时没有附近公开狗友哦～🐶”（圆润字体，淡粉色，字号14px），文字居中。

- 场景2：无任何宠物友好地点点位：地图居中显示可爱草坪插画（尺寸120px×120px），插画下方显示文字“暂时没有找到宠物友好地点哦～”（圆润字体，淡粉色，字号14px），文字居中。

- 场景3：无任何宠物门店点位：地图居中显示可爱小屋插画（尺寸120px×120px），插画下方显示文字“暂时没有找到宠物门店哦～”（圆润字体，淡粉色，字号14px），文字居中。

#### 3\.1\.3\.2 加载状态

- 地图初始化加载：页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示地图及点位。

- 筛选后加载：筛选面板收起后，地图中间显示小狗转圈加载动画，加载完成后，显示筛选后的点位，动画消失。

- 下拉刷新加载：下拉地图时，顶部显示小狗转圈加载动画，刷新完成后，动画消失，地图点位重新加载。

### 3\.1\.4 首页模块子页面（共3个，无遗漏，含所有样式、交互）

#### 子页面1：同片区热门狗友列表页（页面路径：/pages/home/friendList）

- 1\. 页面结构（从上到下）
                  顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回首页地图），中间标题“同片区热门狗友”，右侧无图标。

- 主体内容：用户列表区域（按距离由近到远排序）。

- 底部Tab栏组件（复用全局组件）。

- 2\. 详细样式
                  列表条目样式：每条用户条目为大圆角（12px）卡片，高度80px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距16px，上下条目间距12px；条目内部布局（左\-中\-右）：
                          左侧：圆形用户头像（直径48px），带淡粉色细边框（1px）。

2\. 各区域详细样式与交互逻辑（无省略）

- **顶部导航栏**：严格复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到首页地图页），同时触发全局按压特效（微缩回弹\+震动\+粒子）；中间标题“同片区热门狗友”字数未超过8字，居中显示，字体、颜色符合全局规范；右侧无任何图标，不占位。

- **主体内容：用户列表区域**列表样式：采用纵向卡片列表布局，每个用户为一张独立的通用卡片组件（复用全局卡片组件规范），卡片宽度为屏幕宽度的95%，居中显示，卡片之间间距12px，与页面上下左右间距符合全局布局规范（上下20px、左右16px）。

- 单张用户卡片内部布局（左\-右布局）：
        左侧：圆形宠物头像（直径60px），带淡粉色（\#FFC1E9）细边框（1px），头像下方添加小尺寸标签（大圆角8px，背景淡粉色，白色圆润字体，字号10px），显示宠物品种（如“金毛”）。

- 右侧：占据卡片剩余宽度，内边距左右12px，布局从上到下：
            用户昵称：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，右侧添加距离标签（大圆角8px，背景浅奶黄\#FFF4D2，柔浅蓝\#E0F7FF文字，字号12px），显示“距离XX米”（如“距离500米”），与昵称右对齐。

- 宠物信息：圆润字体，深灰色（\#333333），字号14px，显示“宠物名称\+年龄\+体型”（如“糯米 · 2岁 · 中型犬”），位于昵称下方，间距4px。

- 个性签名：圆润字体，浅灰色（\#E5E5E5），字号12px，可换行，最多显示2行，超出部分显示省略号（…），位于宠物信息下方，间距4px；若用户无个性签名，显示默认文字“暂无个性签名”。

- 操作按钮：两个并排小按钮（圆角8px，高度32px，宽度各占右侧区域的45%），位于卡片底部，间距8px；左侧按钮“关注”（次要按钮样式，背景浅灰色，深灰色文字），右侧按钮“发私信”（主按钮样式，背景淡粉色，白色文字），按钮字体均为圆润字体，字号14px。

列表交互逻辑：
        卡片点击：点击用户卡片任意区域（除操作按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至该用户的他人用户主页详情页。

关注按钮：点击按钮，触发“关注/取消关注”操作，同时触发全局按压特效；未关注时，点击后按钮变为“已关注”（背景淡粉色，白色文字），同时页面实时刷新关注状态；已关注时，点击后弹出确认弹窗（复用全局确认弹窗，提示“确定要取消关注该用户吗？”），点击“确定”取消关注，按钮恢复“关注”状态，页面实时刷新。

发私信按钮：点击按钮，触发跳转操作，同时触发全局按压特效，平滑跳转至与该用户的私信聊天页面；若为陌生人，跳转后自动弹出提示弹窗（“陌生人仅可发送1条私信，对方回复后可正常聊天”）。

列表排序：默认按“距离由近到远”排序，排序规则不可修改，列表顶部不显示排序切换选项。

下拉刷新：下拉列表顶部，触发刷新操作，显示小狗转圈加载动画，刷新完成后，列表重新加载当前片区最新的热门狗友数据，动画消失。

上拉加载：列表滑动至底部，自动触发上拉加载操作，显示小狗转圈加载动画，加载下一页数据；若已加载全部数据，底部显示文字“已加载全部狗友”（圆润字体，淡粉色，字号14px），居中显示，无加载动画。

**底部Tab栏**：严格复用全局底部Tab栏组件，选中状态保持为“首页”（爪印图标选中样式），点击其他Tab项可触发对应页面切换，交互逻辑符合全局规范。

#### 3\. 空状态与加载状态

- **空状态**：当前片区无公开位置的养狗用户时，列表区域居中显示可爱小狗发呆插画（尺寸120px×120px），插画下方显示文字“暂时没有附近公开狗友哦～🐶”（圆润字体，淡粉色，字号14px），文字居中；底部Tab栏正常显示。

- **加载状态**：
    页面初始化加载：页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示用户列表（或空状态）。

- 下拉刷新加载：下拉列表时，顶部显示小狗转圈加载动画，刷新完成后，动画消失，列表重新加载数据。

- 上拉加载：滑动至列表底部，底部显示小狗转圈加载动画，加载完成后，动画消失，显示下一页数据；若已加载全部，显示“已加载全部狗友”文字。

#### 子页面2：附近遛狗好去处列表页（页面路径：/pages/home/placeList）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回首页地图），中间标题“附近遛狗好去处”，右侧搜索图标（尺寸24px×24px，淡粉色\#FFC1E9）。

- 分类筛选栏（横向可滑动）：位于导航栏下方，间距16px，用于切换“宠物友好地点”“宠物门店”两类内容。

- 主体内容：地点/门店列表区域（按距离由近到远排序）。

- 底部Tab栏组件（复用全局组件）。

2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到首页地图页），同时触发全局按压特效；中间标题“附近遛狗好去处”居中显示，符合全局字体、颜色规范；右侧搜索图标点击后，触发搜索操作，弹出搜索弹窗（复用全局选择弹窗样式，内置搜索框），同时触发全局按压特效。

**分类筛选栏**：
        样式：横向可滑动容器，高度44px，背景奶白色（\#FFF9F9），大圆角12px，淡粉色（\#FFC1E9）细边框，宽度为屏幕宽度的95%，居中显示；内部包含两个分类标签，每个标签高度32px， padding左右16px，大圆角8px，标签之间间距8px。

标签样式：未选中时，背景奶白色，淡粉色文字（圆润字体，字号14px）；选中时，背景淡粉色，白色文字（圆润加粗字体，字号14px）；默认选中“宠物友好地点”标签。

交互逻辑：点击任意分类标签，触发分类切换操作，同时触发全局按压特效；切换后，标签切换为选中状态，另一标签变为未选中状态，列表区域实时刷新对应分类的内容（宠物友好地点/宠物门店），刷新时显示小狗转圈加载动画。

**搜索弹窗**：
        样式：复用全局选择弹窗样式，宽度为屏幕85%，顶部标题“搜索地点/门店”，中间为复用全局输入框组件（占位文字“输入地点/门店名称”），底部为“搜索”主按钮（通栏，淡粉色背景，白色文字）。

交互逻辑：输入文字后，点击底部“搜索”按钮，触发搜索操作，同时触发全局按压特效，关闭搜索弹窗，列表区域显示小狗转圈加载动画，加载完成后显示搜索结果（若无结果，显示对应空状态）；点击弹窗空白区域或底部关闭按钮，关闭弹窗，不执行搜索操作。

**主体内容：列表区域（分两类，样式略有差异，交互一致）**通用样式：采用纵向卡片列表布局，每个地点/门店为一张独立的通用卡片组件，卡片宽度为屏幕宽度的95%，居中显示，卡片之间间距12px，与页面上下左右间距符合全局布局规范；卡片内部均包含封面图、名称、核心信息、操作按钮。

分类1：宠物友好地点卡片（默认显示）
            内部布局（从上到下）：
                封面图：大圆角12px，尺寸16:9，宽度全屏，高度自适应（约120px），封面图下方添加标签（大圆角8px，背景浅奶黄\#FFF4D2，柔浅蓝\#E0F7FF文字，字号12px，位置在封面图左下角，距离左下角12px），显示地点类型（如“公园”“草坪”）。

地点名称：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，位于封面图下方，间距8px，左侧添加淡粉色地标图标（尺寸16px×16px），与文字间距4px。

核心信息：圆润字体，深灰色（\#333333），字号14px，位于名称下方，间距4px，显示“距离XX米 · 开放时间：XX:XX\-XX:XX”（如“距离800米 · 开放时间：06:00\-22:00”）；若无开放时间，显示“距离XX米 · 全天开放”。

地点简介：圆润字体，浅灰色（\#E5E5E5），字号12px，位于核心信息下方，间距4px，可换行，最多显示2行，超出部分显示省略号（…）；若无简介，显示默认文字“暂无简介”。

操作按钮：通栏主按钮（大圆角12px，高度44px，背景淡粉色，白色圆润加粗字体，字号16px），文字“立即导航”，位于卡片底部，间距8px。

分类2：宠物门店卡片（切换分类后显示）
            内部布局（从上到下）：
                封面图：与宠物友好地点卡片一致（大圆角12px，16:9尺寸，宽度全屏），封面图下方添加标签（大圆角8px，背景浅奶黄\#FFF4D2，柔浅蓝\#E0F7FF文字，字号12px），显示门店类型（如“宠物店”“宠物医院”“宠物美容”）。

门店名称：与宠物友好地点卡片一致（圆润加粗字体，淡粉色，字号16px，左侧添加淡粉色小屋图标）。

核心信息：圆润字体，深灰色（\#333333），字号14px，位于名称下方，间距4px，显示“距离XX米 · 营业时间：XX:XX\-XX:XX · 地址：XXX”（地址可换行，最多显示1行，超出部分显示省略号）。

特色简介：圆润字体，浅灰色（\#E5E5E5），字号12px，位于核心信息下方，间距4px，可换行，最多显示2行，超出部分显示省略号；若无特色简介，显示默认文字“暂无特色简介”。

操作按钮：两个并排按钮（圆角12px，高度44px，每个按钮宽度为卡片宽度的45%），位于卡片底部，间距8px；左侧“查看详情”（主按钮样式），右侧“立即导航”（主按钮样式），按钮字体均为圆润加粗字体，字号16px，白色文字，背景淡粉色。

列表交互逻辑：
            卡片点击：点击宠物友好地点卡片（除“立即导航”按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至该地点的详情弹窗（与首页地图点位点击后的地点信息弹窗一致）；点击宠物门店卡片（除操作按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至门店详情页。

立即导航按钮：点击按钮，触发导航操作，同时触发全局按压特效，关闭当前页面（或弹窗），唤起微信地图，自动输入对应地点/门店地址，生成导航路线。

查看详情按钮（仅门店卡片有）：点击按钮，触发跳转操作，同时触发全局按压特效，平滑跳转至门店详情页。

列表排序：默认按“距离由近到远”排序，排序规则不可修改，列表顶部不显示排序切换选项。

下拉刷新：下拉列表顶部，触发刷新操作，显示小狗转圈加载动画，刷新完成后，列表重新加载当前分类的最新数据，动画消失。

上拉加载：列表滑动至底部，自动触发上拉加载操作，显示小狗转圈加载动画，加载下一页数据；若已加载全部数据，底部显示文字“已加载全部内容”（圆润字体，淡粉色，字号14px），居中显示，无加载动画。

**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“首页”，交互逻辑符合全局规范。

3\. 空状态与加载状态
    **空状态（分两类）**：
        宠物友好地点空状态：选中“宠物友好地点”标签，无对应数据时，列表区域居中显示可爱草坪插画（尺寸120px×120px），插画下方显示文字“暂时没有找到宠物友好地点哦～”（圆润字体，淡粉色，字号14px），文字居中。

宠物门店空状态：选中“宠物门店”标签，无对应数据时，列表区域居中显示可爱小屋插画（尺寸120px×120px），插画下方显示文字“暂时没有找到宠物门店哦～”（圆润字体，淡粉色，字号14px），文字居中。

搜索空状态：搜索后无对应结果时，列表区域居中显示可爱放大镜插画（尺寸120px×120px），插画下方显示文字“未找到相关地点/门店哦～”（圆润字体，淡粉色，字号14px），文字居中。

**加载状态**：
        页面初始化加载：页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示列表（或空状态）。

分类切换加载：点击分类标签后，列表区域中间显示小狗转圈加载动画，加载完成后，动画消失，显示对应分类列表。

搜索加载：点击搜索按钮后，列表区域中间显示小狗转圈加载动画，加载完成后，动画消失，显示搜索结果（或搜索空状态）。

下拉刷新加载与上拉加载：与同片区热门狗友列表页一致。

#### 子页面3：门店详情页（页面路径：/pages/home/storeDetail）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回上一页，可能是首页地图、附近遛狗好去处列表页），中间标题“门店详情”，右侧无图标。

- 门店封面图区域（全屏宽度）。

- 门店核心信息区域（门店名称、分类、评分、地址、营业时间）。

- 门店详情介绍区域（特色服务、详细简介）。

- 底部固定操作栏（两个并排按钮：立即导航、联系门店）。

- 底部Tab栏组件（复用全局组件）。

2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到上一页），同时触发全局按压特效；中间标题“门店详情”居中显示，符合全局字体、颜色规范；右侧无图标，不占位。

**门店封面图区域**：
        样式：全屏宽度，高度自适应（约200px），封面图为门店实景图，大圆角12px（仅底部两个角为圆角，顶部与导航栏无缝衔接，无圆角）；封面图左下角添加分类标签（大圆角8px，背景浅奶黄\#FFF4D2，柔浅蓝\#E0F7FF文字，字号12px），显示门店类型（如“宠物店”“宠物医院”）。

交互逻辑：点击封面图，触发全屏预览操作，同时触发全局按压特效；全屏预览时，显示封面图原图，支持左右滑动切换（若门店有多张封面图），点击预览页面空白区域或右上角关闭图标，关闭预览，返回详情页。

**门店核心信息区域**：
        样式：大圆角12px卡片，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距16px，宽度为屏幕宽度的95%，居中显示，与封面图间距12px。

内部布局（从上到下）：
            门店名称\+评分：门店名称（圆润加粗字体，淡粉色\#FFC1E9，字号18px），右侧添加评分标签（大圆角8px，背景淡粉色，白色文字，字号14px），显示“4\.8分”（评分范围0\-5分，保留1位小数），与名称右对齐；若暂无评分，显示“暂无评分”。

地址信息：圆润字体，深灰色（\#333333），字号14px，位于名称下方，间距8px，左侧添加淡粉色地址图标（尺寸16px×16px），与文字间距4px，显示“地址：XXX”（地址可换行，最多显示2行，超出部分显示省略号）。

营业时间：圆润字体，深灰色（\#333333），字号14px，位于地址下方，间距4px，左侧添加淡粉色时钟图标（尺寸16px×16px），与文字间距4px，显示“营业时间：XX:XX\-XX:XX”；若为24小时营业，显示“营业时间：24小时营业”。

交互逻辑：点击地址信息，触发导航操作，同时触发全局按压特效，唤起微信地图，自动输入门店地址，生成导航路线。

**门店详情介绍区域**：
        样式：大圆角12px卡片，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，内边距16px，宽度为屏幕宽度的95%，居中显示，与核心信息区域间距12px。

内部布局（从上到下）：
            特色服务标题：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，左侧添加淡粉色星星图标（尺寸16px×16px），与文字间距4px，显示“特色服务”。

特色服务列表：无序列表，位于标题下方，间距8px；每个列表项为圆润字体，深灰色（\#333333），字号14px，左侧添加淡粉色小圆圈图标（尺寸8px×8px），与文字间距8px；可显示多个特色服务，支持垂直滚动；若无特色服务，显示“暂无特色服务”（圆润字体，浅灰色，字号14px）。

详细简介标题：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，左侧添加淡粉色铅笔图标（尺寸16px×16px），与文字间距4px，显示“详细简介”，位于特色服务列表下方，间距12px。

详细简介内容：圆润字体，深灰色（\#333333），字号14px，行间距1\.5倍，可换行，支持垂直滚动；若无详细简介，显示“暂无详细简介”（圆润字体，浅灰色，字号14px）。

交互逻辑：详情介绍区域支持垂直滚动，滚动时顶部导航栏、底部固定操作栏、底部Tab栏始终固定可见。

**底部固定操作栏**：
        样式：固定在页面底部（位于底部Tab栏上方），宽度全屏，高度56px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框（仅顶部有边框）；内部包含两个并排按钮，每个按钮宽度为屏幕宽度的45%，高度44px，大圆角12px，间距8px，居中显示。

按钮样式：两个按钮均为主按钮样式（背景淡粉色\#FFC1E9，白色圆润加粗字体，字号16px）；左侧按钮“立即导航”，右侧按钮“联系门店”（图标\+文字，左侧添加淡粉色电话图标，尺寸16px×16px，与文字间距4px）。

交互逻辑：
            点击“立即导航”：触发导航操作，同时触发全局按压特效，唤起微信地图，自动输入门店地址，生成导航路线。

点击“联系门店”：触发拨打电话操作，同时触发全局按压特效，唤起手机拨号界面，自动输入门店预留电话（若门店无预留电话，弹出提示弹窗，提示“该门店暂无预留电话”）。

**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“首页”，交互逻辑符合全局规范。

3\. 空状态与加载状态
    **加载状态**：页面初始化加载时，页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示门店详情内容；若加载失败，弹出提示弹窗（淡粉色叉号图标，标题“加载失败”，内容“门店详情加载失败，请重试”，底部“重试”按钮），点击“重试”按钮，重新加载页面。

**无详情内容状态**：若门店仅有机本信息（名称、地址、营业时间），无特色服务、详细简介、封面图，对应区域显示默认提示文字（如封面图区域显示默认门店插画，特色服务、详细简介区域显示“暂无相关内容”），样式统一为圆润字体、浅灰色、字号14px，居中显示。

## 3\.2 萌宠圈模块（核心页面，页面路径：/pages/circle/index）

### 3\.2\.1 页面结构（从上到下，完整无省略）

1. 顶部导航栏组件（复用全局组件）
    左侧：返回图标（无操作，因萌宠圈为一级页面）

2. 中间：标题“萌宠圈”

3. 右侧：添加\+图标（点击跳转发布动态页）

4. 动态筛选栏（横向可滑动，仅筛选动态类型）

5. 主体内容：动态列表区域（按发布时间由新到旧排序，最新发布的动态显示在顶部）

6. 底部Tab栏组件（复用全局组件）

萌宠圈作为宠物主社交互动的核心场景，聚焦“分享、互动、共鸣”三大核心，为用户提供便捷的动态发布、浏览、互动渠道，所有页面样式严格遵循全局视觉规范，交互逻辑贴合前文统一标准，确保用户体验连贯一致。以下详细说明各区域样式、交互逻辑及相关子页面规范，无任何细节省略。

### 3\.2\.2 各区域详细样式与交互逻辑（无省略）

#### 3\.2\.2\.1 顶部导航栏（复用全局组件，适配萌宠圈场景）

- 样式：严格复用全局顶部导航栏组件，宽度全屏、高度48px，背景奶白色（\#FFF9F9），底部淡粉色（\#FFC1E9）细边框，布局为“左\-中\-右”三栏，左右留16px安全边距。

- 左侧区域：淡粉色（\#FFC1E9）爪印形状返回图标（尺寸24px×24px），因萌宠圈为一级页面，点击该图标无任何操作（避免误触退出小程序），点击时仍触发全局按压特效（微缩回弹\+震动\+淡粉色爱心粒子），保持交互统一性。

- 中间区域：标题“萌宠圈”，圆润加粗字体，字号18px，颜色淡粉色（\#FFC1E9），居中显示，字数3字，符合全局标题规范（不超过8字）。

- 右侧区域：淡粉色（\#FFC1E9）添加\+图标（尺寸24px×24px），图标为软萌卡通样式，与全局图标风格一致。

- 交互逻辑：点击右侧添加\+图标，触发跳转操作，同时触发全局按压特效，平滑跳转至发布动态页（页面路径：/pages/circle/publish），跳转特效为“柔和滑动\+淡入淡出”，过渡时长0\.3秒；导航栏始终固定在页面顶部，不随页面滚动消失。

#### 3\.2\.2\.2 动态筛选栏（横向可滑动，核心筛选组件，仅筛选动态类型）

- 基础样式：横向可滑动容器，高度44px，背景奶白色（\#FFF9F9），大圆角12px，淡粉色（\#FFC1E9）细边框，宽度为屏幕宽度的95%，居中显示，与顶部导航栏下方间距16px，符合全局布局间距规范（上下20px、左右16px）。

- 内部布局：容器内包含4个筛选标签，标签均匀分布，每个标签高度32px，左右内边距16px，大圆角8px，标签之间间距8px；容器可横向滑动，滑动时无卡顿，滑动到两端时无回弹生硬感，适配不同屏幕尺寸。

- 标签样式（分选中/未选中状态，全局统一）：
    未选中状态：背景奶白色（\#FFF9F9），文字为淡粉色（\#FFC1E9），圆润字体，字号14px，无额外阴影，标签内文字居中显示。

- 选中状态：背景淡粉色（\#FFC1E9），文字为白色，圆润加粗字体，字号14px，标签添加轻微淡粉色阴影（rgba\(255, 193, 233, 0\.3\)），突出选中状态，与全局选中样式保持一致。

- 筛选标签内容（固定4个，不可修改，顺序不可调整）：
    全部动态：默认选中标签，筛选显示所有类型的动态（包含图文、视频、纯文字），标签文字“全部动态”。

- 图文动态：筛选显示仅包含图片\+文字的动态，标签文字“图文动态”，左侧添加淡粉色小相机图标（尺寸16px×16px），与文字间距4px。

- 视频动态：筛选显示仅包含视频\+文字（可选）的动态，标签文字“视频动态”，左侧添加淡粉色小视频图标（尺寸16px×16px），与文字间距4px。

- 纯文字动态：筛选显示仅包含文字、无图片/视频的动态，标签文字“纯文字动态”，左侧添加淡粉色小文字图标（尺寸16px×16px），与文字间距4px。

- 交互逻辑：
    标签点击：点击任意筛选标签，触发筛选切换操作，同时触发全局按压特效（微缩回弹\+震动\+淡粉色爱心粒子）；切换后，当前标签变为选中状态，其他标签变为未选中状态，动态列表区域实时刷新，刷新时显示全局统一的小狗转圈加载动画（淡粉色，下方显示“加载中…”文字），加载完成后动画消失，显示对应类型的动态列表。

- 横向滑动：手指在筛选栏上横向滑动，可切换显示隐藏的标签（适配小屏手机），滑动过程中标签无错位、无卡顿，滑动速度与手指滑动速度匹配，滑动停止后标签自动对齐，避免标签一半显示、一半隐藏。

- 筛选记忆：切换筛选标签后，页面刷新或重新进入萌宠圈页面，仍保留上次选中的筛选标签状态（无需重新选择）；仅当用户退出小程序重新进入时，恢复默认选中“全部动态”标签。

#### 3\.2\.2\.3 主体内容：动态列表区域（核心展示区域）

- 基础样式：采用纵向卡片列表布局，每个动态为一张独立的通用卡片组件（复用全局卡片组件规范），卡片宽度为屏幕宽度的95%，居中显示，卡片之间间距12px，与筛选栏下方间距20px，与页面底部Tab栏上方间距20px，符合全局布局间距规范。

- 列表排序：默认按“发布时间由新到旧”排序，最新发布的动态显示在列表顶部，排序规则不可修改，列表顶部不显示排序切换选项，避免冗余操作，贴合轻量化产品定位。

- 动态卡片通用结构（所有类型动态均包含，从上到下）：
    发布者信息栏（左\-右布局）：
        左侧：圆形用户头像（直径48px），带淡粉色（\#FFC1E9）细边框（1px），头像为用户个人头像（未设置则显示默认可爱小狗头像）；头像下方无额外标签，简洁美观。

- 右侧：占据信息栏剩余宽度，内边距左右8px，布局从上到下：
            用户昵称：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，右侧无额外标签，居中对齐左侧头像。

- 发布时间：圆润字体，浅灰色（\#E5E5E5），字号12px，位于昵称下方，间距4px，显示格式为“XX分钟前”“XX小时前”“昨天 XX:XX”“XX月XX日”（超过30天显示完整日期），自动根据发布时间换算，无需用户手动设置。

动态内容区域（根据动态类型略有差异，核心样式统一）：
        动态文字：圆润字体，深灰色（\#333333），字号14px，行间距1\.5倍，可换行，无字数限制（支持长文本）；文本位于发布者信息栏下方，间距12px，左侧与头像对齐；若动态无文字（仅图片/视频），该区域不显示，避免空白。

动态媒体区域（图片/视频，可选）：位于文字下方（无文字则位于发布者信息栏下方），间距8px；媒体区域宽度与卡片宽度一致，高度自适应，大圆角12px，淡粉色（\#FFC1E9）细边框，边框不突兀。

动态互动栏（固定在卡片底部，左\-中\-右布局）：
        左侧：点赞按钮（图标\+数字），淡粉色爱心图标（尺寸20px×20px），未点赞时为浅灰色，点赞后变为淡粉色；图标右侧显示点赞数（圆润字体，浅灰色，字号12px），无点赞时显示“0”，不隐藏；按钮可点击，触发点赞/取消点赞操作。

中间：评论按钮（图标\+数字），淡粉色评论图标（尺寸20px×20px），图标右侧显示评论数（圆润字体，浅灰色，字号12px），无评论时显示“0”，不隐藏；按钮可点击，触发评论弹窗。

右侧：分享按钮（仅图标），淡粉色分享图标（尺寸20px×20px），无数字显示；按钮可点击，触发分享操作。

不同类型动态卡片细节差异（贴合不同内容场景，保持整体风格统一）：
    类型1：图文动态（最常用类型）
        媒体区域：支持1\-9张图片，图片布局规则：1张图片（全屏宽度，高度自适应，比例1:1）；2\-3张图片（2张并排、3张品字形，每张图片比例1:1）；4\-9张图片（2×2、3×3网格布局，每张图片比例1:1）；图片之间无间距，边框统一为淡粉色细边框；图片加载时显示小狗转圈加载动画（淡粉色，尺寸24px×24px），加载失败时显示默认可爱小狗插画（尺寸80px×80px），居中显示。

交互补充：点击任意图片，触发全屏预览操作，同时触发全局按压特效；全屏预览时，显示图片原图，支持左右滑动切换（多图时），点击预览页面空白区域或右上角关闭图标，关闭预览，返回萌宠圈列表页。

类型2：视频动态
        媒体区域：视频宽度全屏，高度自适应（比例16:9），视频封面图为视频首帧，封面图中央添加淡粉色播放图标（尺寸32px×32px），图标居中显示；视频底部添加进度条（淡粉色，高度2px），播放时进度条同步推进，未播放时进度条为浅灰色。

交互补充：点击视频区域任意位置，触发视频播放/暂停操作，同时触发全局按压特效；播放时，视频自动静音（默认静音，避免突兀），视频右下角显示静音图标（淡粉色，尺寸16px×16px），点击静音图标可切换静音/有声状态；视频播放完成后，自动循环播放；点击视频全屏按钮（右下角，淡粉色，尺寸16px×16px），可切换至全屏播放模式，退出全屏后回到原卡片位置。

类型3：纯文字动态
        无媒体区域，动态文字区域直接位于发布者信息栏下方，间距12px；文字区域背景为浅奶黄（\#FFF4D2），大圆角8px，内边距12px，与文字内容间距适配，增强文字可读性；若文字过长（超过5行），自动显示“展开全文”按钮（淡粉色文字，圆润字体，字号12px），点击按钮可展开全部文字，展开后显示“收起”按钮，点击收起文字。

动态列表交互逻辑（全类型动态通用，无例外）：
    卡片点击：点击动态卡片任意区域（除互动按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至动态详情页（页面路径：/pages/circle/detail），跳转特效符合全局规范。

点赞按钮交互：点击点赞按钮，触发点赞/取消点赞操作，同时触发全局按压特效；未点赞时，点击后图标变为淡粉色，点赞数\+1，伴随3个淡粉色小爱心粒子飘散特效；已点赞时，点击后图标变为浅灰色，点赞数\-1，无粒子特效；点赞状态实时刷新，无需刷新页面。

评论按钮交互：点击评论按钮，触发评论弹窗（复用全局选择弹窗样式，宽度为屏幕85%），同时触发全局按压特效；弹窗内包含评论输入框（复用全局输入框组件，占位文字“写下你的评论吧～”）、评论列表（按发布时间由新到旧排序）、底部“发送”主按钮；评论相关详细交互见“3\.2\.4 核心交互：评论功能”。

分享按钮交互：点击分享按钮，触发分享操作，同时触发全局按压特效，弹出分享弹窗（复用全局选择弹窗样式），弹窗内包含“分享给微信好友”“分享到朋友圈”两个选项，每个选项为图标\+文字（圆润字体，深灰色，字号14px）；点击任意分享选项，触发对应分享操作，分享内容为动态截图\+“我在WaggleWorld分享了萌宠日常，快来看看吧～”文字；点击弹窗空白区域或底部关闭按钮，关闭分享弹窗，不执行分享操作。

下拉刷新：下拉列表顶部，触发刷新操作，显示全局统一的小狗转圈加载动画（淡粉色，下方显示“加载中…”文字），刷新完成后，动画消失，列表重新加载最新动态数据（包含未浏览过的新动态），刷新后列表回到顶部。

上拉加载：列表滑动至底部，自动触发上拉加载操作，显示小狗转圈加载动画，加载下一页动态数据；若已加载全部动态，底部显示文字“已加载全部动态”（圆润字体，淡粉色，字号14px），居中显示，无加载动画，后续上拉不再触发加载操作。

#### 3\.2\.2\.4 底部Tab栏（复用全局组件，适配萌宠圈场景）

- 样式：严格复用全局底部Tab栏组件，宽度全屏、高度56px，背景奶白色（\#FFF9F9），顶部淡粉色（\#FFC1E9）细边框，4个Tab项均匀分布，样式、尺寸、图标完全符合全局规范。

- 选中状态：进入萌宠圈页面后，底部Tab栏“萌宠圈”项自动切换为选中状态（浅粉色朋友圈图标，轻微放大至1\.1倍；文字淡粉色、加粗，字号12px），其他Tab项（首页、消息、我的）为未选中状态，与全局Tab栏选中规则一致。

- 交互逻辑：点击其他Tab项（首页、消息、我的），触发页面切换操作，同时触发全局按压特效，切换至对应页面；重复点击“萌宠圈”Tab项，无任何操作（避免重复跳转）；Tab栏始终固定在页面底部，不随列表滚动消失，保持视觉统一性。

### 3\.2\.3 空状态与加载状态（无省略，贴合萌宠圈场景）

- **空状态（分两类场景，样式统一，贴合软萌风格）**：
    场景1：无任何动态（整个萌宠圈无用户发布动态）：列表区域居中显示可爱小狗举相机插画（尺寸120px×120px），插画下方显示文字“还没有动态哦～快来发布第一条萌宠日常吧🐶”（圆润字体，淡粉色，字号14px），文字居中；顶部导航栏、筛选栏、底部Tab栏正常显示；点击文字或插画，可触发跳转操作，平滑跳转至发布动态页，同时触发全局按压特效。

- 场景2：筛选后无对应动态（如选择“视频动态”，但无用户发布视频动态）：列表区域居中显示对应类型的空白插画（视频动态显示小狗举视频图标插画，纯文字动态显示小狗举文字图标插画），插画尺寸120px×120px，插画下方显示文字“暂时没有找到XX动态哦～”（XX替换为筛选类型，如“视频”“纯文字”），圆润字体，淡粉色，字号14px，文字居中；筛选栏仍显示当前选中标签，可切换其他标签重新筛选。

- **加载状态（分三类场景，复用全局加载特效）**：
    页面初始化加载：进入萌宠圈页面后，页面居中显示小狗转圈加载动画（淡粉色，尺寸40px×40px），下方显示“加载中…”文字（圆润字体，淡粉色，字号14px），加载完成后，动画消失，显示动态列表（或空状态）；若加载失败，弹出提示弹窗（淡粉色叉号图标，标题“加载失败”，内容“萌宠圈动态加载失败，请重试”，底部“重试”按钮），点击“重试”按钮，重新加载页面，同时触发全局按压特效。

- 筛选切换加载：点击筛选标签后，动态列表区域中间显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示对应类型的动态列表（或筛选空状态）；加载失败时，弹出相同的加载失败提示弹窗，支持重试。

- 下拉刷新与上拉加载：与前文列表加载逻辑一致，下拉刷新时顶部显示小狗转圈加载动画，上拉加载时底部显示动画，加载失败时弹出提示弹窗，加载完成后动画消失，显示对应数据（或“已加载全部动态”文字）。

### 3\.2\.4 萌宠圈核心交互：评论功能（全场景覆盖，无省略）

- 评论弹窗样式（复用全局选择弹窗，适配评论场景）：
    基础样式：宽度为屏幕85%，高度自适应（最大高度不超过屏幕60%），背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，大圆角12px，内边距上下20px、左右16px，轻微阴影，符合全局弹窗规范。

- 内部布局（从上到下）：
        弹窗标题：居中显示，圆润加粗字体，淡粉色（\#FFC1E9），字号16px，文字“写下你的评论”，无图标，与下方输入框间距16px。

- 评论输入框：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，占位文字“写下你的评论吧～”（圆润字体，浅灰色，字号14px），输入文字为深灰色，字号14px；输入框支持换行（点击换行键可换行），无字数限制。

- 评论列表：位于输入框下方，间距16px，高度自适应（最大高度为弹窗高度的60%），支持垂直滚动；每个评论为独立条目，左\-右布局，条目之间间距8px；评论列表按发布时间由新到旧排序，最新评论显示在顶部。

- 底部按钮：通栏主按钮（大圆角12px，高度44px，背景淡粉色，白色圆润加粗字体，字号16px），文字“发送”，与评论列表间距16px；输入框为空时，按钮置灰（浅灰色背景，深灰色文字），不可点击，无交互特效；输入文字后，按钮恢复正常状态，可点击。

评论条目样式（统一规范，清晰易读）：
    左\-右布局：左侧为评论者头像（圆形，直径32px，淡粉色细边框，未设置头像则显示默认小狗头像）；右侧为评论内容区域，占据条目剩余宽度，内边距左右8px。

评论内容区域布局（从上到下）：
        评论者昵称：圆润加粗字体，淡粉色（\#FFC1E9），字号14px，与评论内容间距4px。

评论文字：圆润字体，深灰色（\#333333），字号14px，行间距1\.5倍，可换行，无字数限制；若评论者为动态发布者，评论文字右侧添加淡粉色“作者”标签（大圆角8px，背景淡粉色，白色文字，字号12px），与文字间距4px。

评论时间：圆润字体，浅灰色（\#E5E5E5），字号12px，位于评论文字下方，间距4px，显示格式与动态发布时间一致（“XX分钟前”“昨天 XX:XX”等）。

评论交互逻辑（全流程覆盖，无遗漏）：
评论发布：输入评论文字后，点击底部“发送”按钮，触发发布评论操作，同时触发全局按压特效；发布成功后，弹出提示弹窗（淡粉色对勾图标，标题“发布成功”，内容“评论已发布”，底部“确定”按钮），点击“确定”关闭弹窗，评论列表实时刷新，新评论显示在列表顶部，输入框清空，按钮恢复置灰状态；发布失败时，弹出提示弹窗（淡粉色叉号图标，标题“发布失败”，内容“评论发布失败，请重试”，底部“重试”按钮），点击“重试”重新发布。

评论浏览：评论列表支持垂直滚动，滚动时输入框、底部按钮始终固定在弹窗顶部和底部，不随滚动消失；评论过多时，可通过滚动查看全部评论，无卡顿、无错位。

评论关闭：点击弹窗空白区域或弹窗顶部关闭图标（淡粉色，尺寸20px×20px），触发关闭弹窗操作，同时触发全局按压特效，弹窗淡入淡出消失，未发布的评论内容自动保存（再次打开评论弹窗时，输入框显示未发布的内容）；点击“发送”按钮发布成功后，弹窗不自动关闭，需用户手动关闭。

评论删除（仅本人可操作）：点击自己发布的评论条目右侧的淡粉色删除图标（尺寸16px×16px），触发删除确认操作，同时触发全局按压特效，弹出全局确认弹窗（淡粉色疑问图标，标题“确认删除”，内容“确定要删除这条评论吗？”，底部双按钮“取消”“确定”）；点击“确定”，删除该评论，评论列表实时刷新，删除成功后弹出提示弹窗；点击“取消”，关闭确认弹窗，不执行删除操作。

评论回复（支持一级回复，不支持多级回复，贴合轻量化定位）：点击任意评论条目右侧的淡粉色回复图标（尺寸16px×16px），触发回复操作，同时触发全局按压特效；此时评论输入框自动聚焦，占位文字变为“回复 @XXX：”（XXX为被回复者昵称），输入框内自动添加“@XXX ”前缀（不可删除前缀，确保回复对象明确）；输入回复内容后，点击“发送”按钮，发布回复评论，回复成功后，评论列表实时刷新，回复内容显示在被回复评论的下方，左侧添加淡粉色小箭头图标（尺寸12px×12px），与被回复评论区分，回复的显示格式、交互逻辑与普通评论一致（支持删除、浏览）；若未输入回复内容，点击“发送”按钮无操作，按钮保持置灰状态。

评论提醒（关联消息模块）：当用户发布的动态被评论、回复时，系统自动发送消息提醒（消息模块“系统通知”栏），提醒内容格式统一为：“XXX 评论了你的动态：XXX”“XXX 回复了你的评论：XXX”（XXX为评论者昵称、评论/回复内容，内容超出15字显示省略号）；点击该提醒，触发跳转操作，同时触发全局按压特效，平滑跳转至对应动态的详情页，并自动弹出评论弹窗，定位到对应评论/回复位置，方便用户快速查看、回复。

### 3\.2\.5 萌宠圈模块子页面（共3个，无遗漏，含所有样式、交互）

#### 子页面1：发布动态页（页面路径：/pages/circle/publish）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回萌宠圈主页面），中间标题“发布动态”，右侧“预览”图标（淡粉色，尺寸24px×24px）。


- 动态类型选择栏（横向排列，3个选项，固定）。

- 主体内容：动态编辑区域（文字输入框\+媒体上传区域）。


- 底部固定发布栏（通栏发布按钮）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到萌宠圈主页面），同时触发全局按压特效；若动态编辑区域有未保存内容（输入文字、上传媒体），返回前弹出确认弹窗（复用全局确认弹窗，提示“确定要放弃编辑吗？未保存内容将丢失”，底部双按钮“取消”“确定”），点击“确定”放弃编辑并返回，点击“取消”留在当前页面；中间标题“发布动态”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧“预览”图标为软萌卡通样式，淡粉色，点击后触发预览操作，同时触发全局按压特效，平滑跳转至动态预览页，预览完成后可返回编辑页继续修改。

**动态类型选择栏**：
    样式：横向排列，宽度为屏幕宽度的95%，居中显示，与导航栏下方间距16px；包含3个类型选项，每个选项为大圆角（8px）卡片，高度36px，宽度均匀分布，淡粉色细边框，背景奶白色；未选中时，文字淡粉色、圆润字体、字号14px，左侧添加对应图标（图文：相机图标、视频：视频图标、纯文字：文字图标）；选中时，背景淡粉色、文字白色、圆润加粗字体、字号14px，图标变为白色，与全局选中样式一致。


类型选项（固定3个，不可修改，顺序不可调整）：
    图文动态：默认选中，点击后可同时输入文字、上传图片；
    视频动态：点击后可输入文字（可选）、上传视频；
    纯文字动态：点击后仅可输入文字，无媒体上传区域。


交互逻辑：点击任意类型选项，触发类型切换操作，同时触发全局按压特效；切换后，当前选项变为选中状态，其他选项变为未选中状态，主体内容区域实时切换（显示/隐藏媒体上传区域），切换时无加载动画，即时显示对应编辑区域，提升操作流畅度。

**主体内容：动态编辑区域**
    文字输入框：复用全局输入框组件，宽度为屏幕宽度的95%，居中显示，高度自适应（默认高度44px，输入文字后自动换行，高度最大不超过屏幕30%），大圆角12px，背景奶白色，淡粉色细边框；占位文字“分享你的萌宠日常吧～”（圆润字体，浅灰色，字号14px），输入文字为深灰色，圆润字体，字号14px，行间距1\.5倍，无字数限制；输入时，输入框自动聚焦，弹出微信自带键盘（适配小程序规范），键盘样式贴合软萌风格（可设置为浅色键盘）。


媒体上传区域（图文/视频类型显示，纯文字类型隐藏）：
    样式：宽度与文字输入框一致，高度自适应，与输入框下方间距16px；上传区域为大圆角12px卡片，淡粉色细边框，背景奶白色，内边距16px，居中显示；区域内显示上传提示图标（图文：淡粉色相机图标，尺寸48px×48px；视频：淡粉色视频图标，尺寸48px×48px），下方显示提示文字“点击上传图片/视频”（圆润字体，淡粉色，字号14px），居中显示。


上传规则（贴合微信小程序规范，兼顾易用性）：
    图文动态：支持上传1\-9张图片，格式为JPG、PNG，单张图片大小不超过5MB；上传后，图片按前文图文动态布局规则显示在上传区域，每张图片右上角添加淡粉色删除图标（尺寸20px×20px），点击删除图标可删除对应图片，同时触发全局按压特效；图片上传时显示小狗转圈加载动画（淡粉色，尺寸24px×24px），加载失败时显示默认小狗插画，点击可重新上传。


视频动态：支持上传1个视频，格式为MP4，视频时长1\-60秒，大小不超过20MB；上传后，视频显示封面图（首帧），封面图中央添加淡粉色播放图标，下方显示视频时长（圆润字体，浅灰色，字号12px）；视频右上角添加淡粉色删除图标，点击可删除视频，触发全局按压特效；视频上传时显示小狗转圈加载动画，加载失败时弹出提示弹窗，点击可重新上传；支持拍摄上传（点击上传区域，弹出选择弹窗，选项为“拍照/拍摄视频”“从相册选择”），拍摄时调用微信相机，适配小程序相机权限规范。


**底部固定发布栏**：
    样式：固定在页面底部（位于底部Tab栏上方），宽度全屏，高度56px，背景奶白色，淡粉色细边框（仅顶部有边框）；内部包含通栏发布按钮，大圆角12px，高度44px，背景淡粉色，白色圆润加粗字体，字号16px，文字“发布”，居中显示。


交互逻辑：发布按钮置灰规则（不可点击）：文字输入框为空且无媒体上传（图文/视频类型）；仅当文字输入框有内容，或有媒体上传（图文/视频类型）时，按钮恢复正常状态，可点击；点击“发布”按钮，触发发布动态操作，同时触发全局按压特效，弹出加载弹窗（复用全局加载弹窗，小狗转圈动画\+“发布中…”文字）；发布成功后，加载弹窗消失，弹出提示弹窗（淡粉色对勾图标，标题“发布成功”，内容“动态已发布至萌宠圈”，底部“确定”按钮），点击“确定”，自动跳转回萌宠圈主页面，列表实时刷新，新发布的动态显示在顶部；发布失败时，加载弹窗消失，弹出提示弹窗（淡粉色叉号图标，标题“发布失败”，内容“动态发布失败，请检查网络或媒体格式后重试”，底部“重试”按钮），点击“重试”重新发布。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“萌宠圈”，点击其他Tab项可触发对应页面切换，交互逻辑符合全局规范；发布页面底部Tab栏正常显示，不隐藏。


3\. 空状态与加载状态
    **空编辑状态**：进入发布页面后，文字输入框为空、无媒体上传时，媒体上传区域显示默认提示图标和文字，文字输入框显示占位文字，发布按钮置灰，整体布局整洁，无冗余元素。


**加载状态**：
    媒体上传加载：上传图片/视频时，对应媒体位置显示小狗转圈加载动画，加载完成后显示图片/视频，动画消失；加载失败时，显示默认插画或弹出提示弹窗。


发布加载：点击发布按钮后，页面居中显示加载弹窗，直至发布完成（成功/失败），加载弹窗自动消失；发布过程中，不可点击任何按钮，避免重复操作。


#### 子页面2：动态详情页（页面路径：/pages/circle/detail）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回萌宠圈主页面或上一页），中间标题“动态详情”，右侧无图标。


- 动态主体卡片（与萌宠圈列表页动态卡片样式一致，细节更完整）。


- 评论区域（与评论弹窗样式一致，固定显示，无需弹窗触发）。


- 底部固定评论输入栏（输入框\+发送按钮）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）**顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到上一页，可能是萌宠圈主页面、发布预览页），同时触发全局按压特效；中间标题“动态详情”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧无图标，不占位；导航栏始终固定在页面顶部，不随页面滚动消失。


**动态主体卡片**：
    样式：与萌宠圈列表页动态卡片完全一致（发布者信息、动态内容、媒体区域、互动栏），宽度为屏幕宽度的95%，居中显示，与导航栏下方间距20px；细节补充：发布者信息栏右侧添加“关注”按钮（大圆角8px，高度32px，宽度80px，未关注时为次要按钮样式，已关注时为主按钮样式），方便用户直接关注发布者；互动栏的点赞、评论数实时同步，与列表页保持一致。


交互逻辑：
    发布者信息交互：点击发布者头像或昵称，触发跳转操作，同时触发全局按压特效，平滑跳转至该用户的他人用户主页详情页；点击“关注”按钮，触发关注/取消关注操作，同时触发全局按压特效，按钮状态实时切换，页面同步刷新。


媒体区域交互：与列表页一致，图片支持全屏预览、视频支持播放/暂停及全屏播放，交互特效符合全局规范。

互动栏交互：点赞、评论、分享按钮交互与列表页完全一致；点击评论按钮，不弹出弹窗，直接定位到页面下方的评论区域，评论输入框自动聚焦，提升交互效率。


**评论区域**：
    样式：宽度与动态主体卡片一致，居中显示，与动态卡片下方间距20px；布局与评论弹窗一致，包含评论列表（按发布时间由新到旧排序），无弹窗标题，评论列表高度自适应，支持垂直滚动；评论条目样式、交互逻辑（删除、回复）与评论弹窗完全一致，无需额外弹窗，直接在页面内操作。


**底部固定评论输入栏**：
    样式：固定在页面底部（位于底部Tab栏上方），宽度全屏，高度56px，背景奶白色，淡粉色细边框（仅顶部有边框）；内部包含输入框和发送按钮，左\-右布局，输入框占据80%宽度，发送按钮占据20%宽度。


输入框：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，占位文字“写下你的评论吧～”，输入文字规范与前文一致；发送按钮：主按钮样式，大圆角12px，高度44px，背景淡粉色，白色圆润加粗字体，字号14px，文字“发送”；输入框为空时，发送按钮置灰，不可点击；输入文字后，按钮恢复正常状态。


交互逻辑：输入评论文字后，点击“发送”按钮，发布评论，交互逻辑与评论弹窗一致（发布成功/失败提示、列表实时刷新）；点击输入框，弹出微信键盘，输入完成后点击键盘收起按钮，键盘消失，输入框仍保持聚焦状态；支持回复功能，点击评论条目右侧的回复图标，输入框自动添加“@XXX ”前缀，输入回复内容后点击发送，回复内容显示在对应评论下方。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“萌宠圈”，交互逻辑符合全局规范；详情页面底部Tab栏正常显示，不隐藏。


3\. 空状态与加载状态
    **空状态**：动态无评论时，评论区域居中显示可爱小狗评论图标插画（尺寸100px×100px），下方显示文字“暂无评论，快来抢沙发吧～”（圆润字体，淡粉色，字号14px），文字居中；底部评论输入栏正常显示，可正常发布评论。


**加载状态**：
    页面初始化加载：进入详情页后，页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示动态主体卡片、评论区域（或评论空状态）；加载失败时，弹出提示弹窗，支持重试。

评论加载：评论过多时，滚动至评论区域底部，自动触发上拉加载操作，显示小狗转圈加载动画，加载下一页评论；若已加载全部评论，底部显示“已加载全部评论”文字（圆润字体，淡粉色，字号14px），居中显示。


#### 子页面3：动态预览页（页面路径：/pages/circle/preview）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回发布动态编辑页），中间标题“动态预览”，右侧“发布”按钮（主按钮样式，大圆角8px，高度32px，宽度80px，背景淡粉色，白色文字，字号14px）。


- 动态预览卡片（与动态详情页卡片完全一致，显示编辑后的最终效果）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
**顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到发布动态编辑页），同时触发全局按压特效，编辑页保留之前的编辑内容（文字、媒体）；中间标题“动态预览”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧“发布”按钮点击后，触发发布动态操作，交互逻辑与发布动态页“发布”按钮一致（加载弹窗、发布成功/失败提示、跳转至萌宠圈主页面）。


**动态预览卡片**：
    样式：与动态详情页主体卡片完全一致，宽度为屏幕宽度的95%，居中显示，与导航栏下方间距20px；卡片显示内容为发布动态编辑页的当前编辑内容（文字、图片/视频），格式、样式与发布后一致，便于用户查看最终效果；互动栏的点赞、评论、分享按钮仅作展示，不可点击（预览状态无真实数据）。


交互逻辑：媒体区域交互与详情页一致（图片全屏预览、视频播放），仅用于查看效果，不触发真实互动；发布者信息显示当前登录用户的信息（头像、昵称），与实际发布后一致。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“萌宠圈”，点击其他Tab项可触发对应页面切换，但预览状态下，建议用户先完成发布或返回编辑，避免误操作；Tab栏正常显示，不隐藏。


3\. 加载状态
    页面初始化加载：进入预览页后，页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示动态预览卡片；加载失败时，弹出提示弹窗（提示“预览失败，请返回编辑页重试”），点击“重试”重新加载预览页，或点击“返回”回到编辑页。


补充说明：预览页仅用于查看动态发布后的最终效果，不支持任何编辑操作（不可修改文字、删除媒体），所有编辑操作需返回发布动态编辑页完成；若在预览页点击右侧“发布”按钮发布成功，编辑页的未保存内容会自动清空，避免重复发布。

## 3\.3 消息模块（核心页面，页面路径：/pages/message/index）

### 3\.3\.1 页面结构（从上到下，完整无省略）

1. 顶部导航栏组件（复用全局组件）
    左侧：返回图标（无操作，因消息页为一级页面）

2. 中间：标题“消息”


3. 右侧：无图标


4. 消息分类栏（横向可滑动，2个分类，固定）


5. 主体内容：消息列表区域（按消息接收时间由新到旧排序，最新消息显示在顶部）


6. 底部Tab栏组件（复用全局组件）


消息模块作为用户互动通知的核心场景，聚焦“及时提醒、便捷查看、快速回复”三大核心，涵盖系统通知、好友私信两类核心消息，所有页面样式严格遵循全局视觉规范，交互逻辑贴合前文统一标准，确保用户能快速获取互动信息、高效回复，以下详细说明各区域样式、交互逻辑及相关子页面规范，无任何细节省略。

### 3\.3\.2 各区域详细样式与交互逻辑（无省略）

#### 3\.3\.2\.1 顶部导航栏（复用全局组件，适配消息场景）

- 样式：严格复用全局顶部导航栏组件，宽度全屏、高度48px，背景奶白色（\#FFF9F9），底部淡粉色（\#FFC1E9）细边框，布局为“左\-中\-右”三栏，左右留16px安全边距。


- 左侧区域：淡粉色（\#FFC1E9）爪印形状返回图标（尺寸24px×24px），因消息页为一级页面，点击该图标无任何操作（避免误触退出小程序），点击时仍触发全局按压特效（微缩回弹\+震动\+淡粉色爱心粒子），保持交互统一性。


- 中间区域：标题“消息”，圆润加粗字体，字号18px，颜色淡粉色（\#FFC1E9），居中显示，字数2字，符合全局标题规范（不超过8字）。


- 右侧区域：无任何图标，不占位，保持页面简洁，避免冗余元素。


- 交互逻辑：导航栏始终固定在页面顶部，不随页面滚动消失；无额外交互操作，仅作标题展示和视觉引导。


#### 3\.3\.2\.2 消息分类栏（横向可滑动，核心分类组件，仅2个分类）

- 基础样式：横向可滑动容器，高度44px，背景奶白色（\#FFF9F9），大圆角12px，淡粉色（\#FFC1E9）细边框，宽度为屏幕宽度的95%，居中显示，与顶部导航栏下方间距16px，符合全局布局间距规范（上下20px、左右16px）。


- 内部布局：容器内包含2个分类标签，标签均匀分布，每个标签高度32px，左右内边距24px，大圆角8px，标签之间间距8px；容器可横向滑动（适配小屏手机），滑动时无卡顿，滑动到两端时无回弹生硬感，标签始终居中对齐。


- 标签样式（分选中/未选中状态，全局统一）：
    未选中状态：背景奶白色（\#FFF9F9），文字为淡粉色（\#FFC1E9），圆润字体，字号14px，无额外阴影，标签内文字居中显示。

- 选中状态：背景淡粉色（\#FFC1E9），文字为白色，圆润加粗字体，字号14px，标签添加轻微淡粉色阴影（rgba\(255, 193, 233, 0\.3\)），突出选中状态，与全局选中样式保持一致。


- 分类标签内容（固定2个，不可修改，顺序不可调整）：
    系统通知：默认选中标签，筛选显示系统发送的通知消息（如动态被评论、被回复、关注提醒等），标签文字“系统通知”，左侧添加淡粉色铃铛图标（尺寸16px×16px），与文字间距4px。


- 好友私信：筛选显示与其他用户的私信聊天消息，标签文字“好友私信”，左侧添加淡粉色对话图标（尺寸16px×16px），与文字间距4px。


- 交互逻辑：
    标签点击：点击任意分类标签，触发分类切换操作，同时触发全局按压特效（微缩回弹\+震动\+淡粉色爱心粒子）；切换后，当前标签变为选中状态，其他标签变为未选中状态，消息列表区域实时刷新，刷新时显示全局统一的小狗转圈加载动画（淡粉色，下方显示“加载中…”文字），加载完成后动画消失，显示对应分类的消息列表。


- 横向滑动：手指在分类栏上横向滑动，可切换显示隐藏的标签（适配小屏手机），滑动过程中标签无错位、无卡顿，滑动速度与手指滑动速度匹配，滑动停止后标签自动对齐。


- 分类记忆：切换分类标签后，页面刷新或重新进入消息页面，仍保留上次选中的分类标签状态（无需重新选择）；仅当用户退出小程序重新进入时，恢复默认选中“系统通知”标签。


- 未读消息提示：若某分类下有未读消息，标签右上角添加红色小圆点（直径8px），突出提醒用户；点击该分类标签，进入消息列表后，未读消息小圆点自动消失，同时更新未读消息状态。


#### 3\.3\.2\.3 主体内容：消息列表区域（核心展示区域）

- 基础样式：采用纵向卡片列表布局，每个消息为一张独立的通用卡片组件（复用全局卡片组件规范），卡片宽度为屏幕宽度的95%，居中显示，卡片之间间距12px，与分类栏下方间距20px，与页面底部Tab栏上方间距20px，符合全局布局间距规范。


- 列表排序：默认按“消息接收时间由新到旧”排序，最新接收的消息显示在列表顶部，排序规则不可修改，列表顶部不显示排序切换选项，贴合消息查看习惯。


- 消息卡片通用结构（所有类型消息均包含，从上到下，左\-右布局）：
    左侧：消息图标（圆形，直径48px，淡粉色细边框，背景奶白色），根据消息类型显示对应图标（系统通知：淡粉色铃铛图标；好友私信：淡粉色对话图标或对方用户头像）。


- 右侧：占据卡片剩余宽度，内边距左右8px，布局从上到下：
    消息标题：圆润加粗字体，淡粉色（\#FFC1E9），字号16px，居中对齐左侧图标；系统通知标题为“系统通知”，好友私信标题为对方用户昵称。


- 消息内容：圆润字体，深灰色（\#333333），字号14px，位于标题下方，间距4px，可换行，最多显示2行，超出部分显示省略号（…）；系统通知内容为具体通知详情（如“XXX评论了你的动态：XXX”），好友私信内容为最新一条私信消息内容。


- 消息时间：圆润字体，浅灰色（\#E5E5E5），字号12px，位于消息内容右侧，右上角对齐，显示格式为“XX分钟前”“XX小时前”“昨天 XX:XX”“XX月XX日”（超过30天显示完整日期），自动根据接收时间换算。


- 未读标记：未读消息卡片，右侧添加淡粉色竖线（宽度4px，高度与消息内容区域一致），同时消息内容字体变为加粗样式，突出未读状态；已读消息无未读标记，消息内容字体恢复正常。


不同类型消息卡片细节差异（贴合不同消息场景，保持整体风格统一）：
    类型1：系统通知消息
        左侧图标：固定为淡粉色铃铛图标（尺寸24px×24px），圆形背景奶白色，淡粉色细边框，居中显示；无其他变化，统一样式。


消息内容规范：根据通知类型显示对应内容，格式统一，清晰易懂，具体如下：
        动态被评论：“XXX 评论了你的动态：XXX”（XXX为评论者昵称、评论内容，内容超出15字显示省略号）；
        动态被回复：“XXX 回复了你的评论：XXX”（XXX为回复者昵称、回复内容，内容超出15字显示省略号）；
        被关注：“XXX 关注了你，快去看看吧～”；
        动态审核通知：“你的动态已通过审核，可在萌宠圈查看”“你的动态未通过审核，原因：XXX”；
        系统公告：“【系统公告】XXX”（XXX为公告内容，超出20字显示省略号）。


类型2：好友私信消息
        左侧图标：显示对方用户头像（圆形，直径48px，淡粉色细边框），对方未设置头像则显示默认可爱小狗头像；与系统通知图标区分，直观识别私信对象。


消息内容规范：显示最新一条私信消息内容，格式如下：
        文字消息：直接显示文字内容，超出20字显示省略号；
        图片消息：显示“\[图片\]”文字，搭配淡粉色小相机图标（尺寸12px×12px）；
        若对方发送多条消息未读，消息内容显示“XXX条未读消息”（XXX为未读消息数量），同时未读标记保留。

消息列表交互逻辑（全类型消息通用，无例外）：
    卡片点击：点击消息卡片任意区域，触发跳转操作，同时触发全局按压特效，平滑跳转至对应消息详情页（系统通知详情页/私信聊天页），跳转特效符合全局规范；点击未读消息卡片后，该消息自动变为已读状态，未读标记消失。


消息删除：长按任意消息卡片，触发删除确认操作，同时触发全局按压特效，弹出全局确认弹窗（淡粉色疑问图标，标题“确认删除”，内容“确定要删除这条消息吗？删除后不可恢复”，底部双按钮“取消”“确定”）；点击“确定”，删除该消息，消息列表实时刷新；点击“取消”，关闭确认弹窗，不执行删除操作；支持批量删除（长按任意消息卡片，进入批量选择模式，卡片左侧显示勾选框，勾选多个消息后，点击底部“删除选中”按钮，批量删除，触发相同确认弹窗）。


下拉刷新：下拉列表顶部，触发刷新操作，显示全局统一的小狗转圈加载动画（淡粉色，下方显示“加载中…”文字），刷新完成后，动画消失，列表重新加载最新消息数据（包含未浏览过的新消息），刷新后列表回到顶部。


上拉加载：列表滑动至底部，自动触发上拉加载操作，显示小狗转圈加载动画，加载下一页消息数据；若已加载全部消息，底部显示文字“已加载全部消息”（圆润字体，淡粉色，字号14px），居中显示，无加载动画，后续上拉不再触发加载操作。


全部已读：点击消息列表顶部的“全部已读”按钮（淡粉色文字，圆润字体，字号14px，位于分类栏下方、列表上方，居右显示），触发全部已读操作，同时触发全局按压特效；点击后，所有未读消息变为已读状态，未读标记消失，按钮变为灰色，不可点击（无未读消息时，按钮默认灰色，不可点击）。


#### 3\.3\.2\.4 底部Tab栏（复用全局组件，适配消息场景）

- 样式：严格复用全局底部Tab栏组件，宽度全屏、高度56px，背景奶白色（\#FFF9F9），顶部淡粉色（\#FFC1E9）细边框，4个Tab项均匀分布，样式、尺寸、图标完全符合全局规范。


- 选中状态：进入消息页面后，底部Tab栏“消息”项自动切换为选中状态（浅粉色铃铛图标，轻微放大至1\.1倍；文字淡粉色、加粗，字号12px），其他Tab项（首页、萌宠圈、我的）为未选中状态，与全局Tab栏选中规则一致；若消息页面有未读消息，消息Tab项右上角添加红色小圆点（直径8px），突出提醒。


- 交互逻辑：点击其他Tab项（首页、萌宠圈、我的），触发页面切换操作，同时触发全局按压特效，切换至对应页面；重复点击“消息”Tab项，无任何操作（避免重复跳转）；Tab栏始终固定在页面底部，不随列表滚动消失，保持视觉统一性。


### 3\.3\.3 空状态与加载状态（无省略，贴合消息场景）

- **空状态（分两类场景，样式统一，贴合软萌风格）**：
    场景1：无任何消息（整个消息模块无系统通知、无好友私信）：列表区域居中显示可爱小狗举铃铛插画（尺寸120px×120px），插画下方显示文字“暂无消息哦～快来和狗友互动吧🐶”（圆润字体，淡粉色，字号14px），文字居中；顶部导航栏、分类栏、底部Tab栏正常显示；点击文字或插画，可触发跳转操作，平滑跳转至萌宠圈主页面，同时触发全局按压特效。


- 场景2：分类后无对应消息（如选择“好友私信”，但无任何私信消息）：列表区域居中显示对应类型的空白插画（好友私信显示小狗对话图标插画），插画尺寸120px×120px，插画下方显示文字“暂时没有XX消息哦～”（XX替换为分类类型，如“系统通知”“好友私信”），圆润字体，淡粉色，字号14px，文字居中；分类栏仍显示当前选中标签，可切换其他标签重新查看。


- **加载状态（分三类场景，复用全局加载特效）**：
    页面初始化加载：进入消息页面后，页面居中显示小狗转圈加载动画（淡粉色，尺寸40px×40px），下方显示“加载中…”文字（圆润字体，淡粉色，字号14px），加载完成后，动画消失，显示消息列表（或空状态）；若加载失败，弹出提示弹窗（淡粉色叉号图标，标题“加载失败”，内容“消息加载失败，请重试”，底部“重试”按钮），点击“重试”按钮，重新加载页面，同时触发全局按压特效。


- 分类切换加载：点击分类标签后，消息列表区域中间显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示对应类型的消息列表（或分类空状态）；加载失败时，弹出相同的加载失败提示弹窗，支持重试。


- 下拉刷新与上拉加载：与前文列表加载逻辑一致，下拉刷新时顶部显示小狗转圈加载动画，上拉加载时底部显示动画，加载失败时弹出提示弹窗，加载完成后动画消失，显示对应数据（或“已加载全部消息”文字）。


### 3\.3\.4 消息模块子页面（共2个，无遗漏，含所有样式、交互）

#### 子页面1：系统通知详情页（页面路径：/pages/message/systemDetail）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回消息主页面），中间标题“系统通知”，右侧无图标。


- 主体内容：系统通知详情列表（按接收时间由新到旧排序）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到消息主页面），同时触发全局按压特效；中间标题“系统通知”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧无图标，不占位；导航栏始终固定在页面顶部，不随页面滚动消失。


**主体内容：系统通知详情列表**
    基础样式：采用纵向列表布局，每个通知详情为独立条目，宽度为屏幕宽度的95%，居中显示，条目之间间距16px，与导航栏下方间距20px，与底部Tab栏上方间距20px；条目无边框、无背景，仅通过间距和文字样式区分，保持简洁。


通知详情条目样式（统一规范，清晰易读）：
    布局从上到下：
        通知时间：圆润字体，浅灰色（\#E5E5E5），字号12px，居中显示，与下方通知内容间距8px；显示格式与消息列表一致（“XX分钟前”“昨天 XX:XX”等）。


通知内容：圆润字体，深灰色（\#333333），字号14px，行间距1\.5倍，可换行，无字数限制；根据通知类型显示对应详细内容（如动态被评论通知，显示完整评论内容；动态审核未通过，显示完整原因）；若通知关联动态，内容中动态相关文字（如动态标题、评论内容）变为淡粉色，突出显示。


关联跳转：若通知关联动态（如动态被评论、被回复、审核通知），点击通知内容中的淡粉色文字，触发跳转操作，同时触发全局按压特效，平滑跳转至对应动态的详情页；无关联内容的通知（如系统公告、被关注通知），点击无额外操作，仅作查看。


交互逻辑：
    列表滚动：通知详情列表支持垂直滚动，滚动时顶部导航栏、底部Tab栏始终固定可见；通知过多时，可通过滚动查看全部通知，无卡顿、无错位。


通知删除：长按任意通知条目，触发删除确认操作，同时触发全局按压特效，弹出全局确认弹窗（淡粉色疑问图标，标题“确认删除”，内容“确定要删除这条通知吗？删除后不可恢复”，底部双按钮“取消”“确定”）；点击“确定”，删除该通知，列表实时刷新；点击“取消”，关闭确认弹窗，不执行删除操作；支持批量删除（长按任意通知条目，进入批量选择模式，条目左侧显示勾选框，勾选多个通知后，点击底部“删除选中”按钮，批量删除，触发相同确认弹窗）。


全部已读：点击列表顶部的“全部已读”按钮（淡粉色文字，圆润字体，字号14px，居右显示），触发全部已读操作，同时触发全局按压特效；点击后，所有未读通知变为已读状态，按钮变为灰色，不可点击（无未读通知时，按钮默认灰色，不可点击）。


下拉刷新与上拉加载：与消息主页面列表逻辑一致，下拉刷新加载最新通知，上拉加载历史通知，加载完成后显示对应数据或“已加载全部通知”文字。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“消息”，交互逻辑符合全局规范；详情页面底部Tab栏正常显示，不隐藏。


3\. 空状态与加载状态
    **空状态**：无任何系统通知时，列表区域居中显示可爱小狗举铃铛插画（尺寸120px×120px），下方显示文字“暂无系统通知哦～”（圆润字体，淡粉色，字号14px），文字居中；顶部导航栏、底部Tab栏正常显示。


**加载状态**：
    页面初始化加载：进入详情页后，页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示通知列表（或空状态）；加载失败时，弹出提示弹窗，支持重试。


下拉刷新与上拉加载：与消息主页面一致，加载动画、失败提示规范统一。


#### 子页面2：私信聊天页（页面路径：/pages/message/chat）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回消息主页面），中间显示对方用户昵称，右侧对方用户头像（圆形，直径32px，淡粉色细边框）。


- 主体内容：聊天消息区域（按发送时间由旧到新排序，最新消息显示在底部）。


- 底部固定聊天输入栏（输入框\+发送按钮\+表情图标）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，宽度全屏、高度48px，背景奶白色，底部淡粉色细边框；左侧返回图标点击后，触发页面返回操作（回到消息主页面），同时触发全局按压特效；中间对方用户昵称，圆润加粗字体，淡粉色，字号16px，居中显示，若昵称过长（超过8字），显示前6字\+省略号；右侧对方用户头像，圆形，直径32px，淡粉色细边框，点击头像可触发跳转操作，同时触发全局按压特效，平滑跳转至对方用户的他人用户主页详情页；导航栏始终固定在页面顶部，不随聊天区域滚动消失。


**主体内容：聊天消息区域**基础样式：宽度全屏，高度自适应（占据页面中间大部分区域），背景为淡奶白（\#FFF9F9），内边距左右16px、上下20px；聊天消息按“左\-右”布局区分发送方和接收方，发送方消息居右，接收方消息居左，消息之间间距12px；聊天区域支持垂直滚动，滚动时自动定位到最新一条消息（进入页面时默认定位到底部）。


聊天消息样式（分发送方、接收方，样式区分清晰，贴合软萌风格）：
    接收方消息（对方发送）：
        布局：居左，左\-右布局（对方头像\+消息气泡）；对方头像圆形，直径36px，淡粉色细边框，位于消息气泡左侧，间距8px；消息气泡为大圆角12px，背景奶白色，淡粉色细边框，内边距12px，最大宽度为屏幕宽度的60%；消息文字为深灰色，圆润字体，字号14px，行间距1\.5倍，可换行。


发送方消息（自己发送）：
        布局：居右，右\-左布局（消息气泡\+自己头像）；自己头像圆形，直径36px，淡粉色细边框，位于消息气泡右侧，间距8px；消息气泡为大圆角12px，背景淡粉色，内边距12px，最大宽度为屏幕宽度的60%；消息文字为白色，圆润字体，字号14px，行间距1\.5倍，可换行。


消息类型样式（支持文字、图片两种类型，适配聊天场景）：
    文字消息：按上述发送方/接收方样式显示，仅包含文字内容，无额外元素。


图片消息：消息气泡内显示图片，大圆角12px，淡粉色细边框（接收方）/无边框（发送方）；图片尺寸自适应，最大宽度为屏幕宽度的50%，高度自适应，比例保持1:1；图片加载时显示小狗转圈加载动画（淡粉色，尺寸24px×24px），加载失败时显示默认小狗插画，点击可重新加载；点击图片，触发全屏预览操作，同时触发全局按压特效，预览逻辑与萌宠圈图片预览一致（支持左右滑动、点击关闭）。


消息时间：每条消息下方（或相邻两条消息间隔超过10分钟）显示消息时间，圆润字体，浅灰色，字号12px，居中显示；发送方消息时间位于消息气泡下方右侧，接收方消息时间位于消息气泡下方左侧，不突兀。


交互逻辑：
    消息滚动：聊天区域支持垂直滚动，手指滑动可查看历史消息，滚动流畅无卡顿；进入页面时，自动滚动至最新一条消息；滚动到顶部时，显示“已加载全部历史消息”文字（圆润字体，淡粉色，字号12px），居中显示。


消息删除：长按任意消息，触发删除确认操作，同时触发全局按压特效，弹出全局确认弹窗（淡粉色疑问图标，标题“确认删除”，内容“确定要删除这条消息吗？删除后仅自己可见删除，对方仍能看到”，底部双按钮“取消”“确定”）；点击“确定”，删除该消息（仅自己端删除，对方端不影响），聊天区域实时刷新；点击“取消”，关闭确认弹窗，不执行删除操作；仅可删除自己发送的消息，对方发送的消息不可删除。


消息撤回：自己发送的消息，发送后5分钟内可撤回；长按自己发送的消息，弹出“撤回”选项（淡粉色文字，圆润字体，字号14px），点击“撤回”，触发撤回操作，同时触发全局按压特效；撤回成功后，该消息变为“你撤回了一条消息”（圆润字体，浅灰色，字号14px，居中显示），对方端同步显示“对方撤回了一条消息”；超过5分钟，无“撤回”选项，不可撤回。


对方在线状态：顶部导航栏对方用户昵称右侧，添加在线状态标识（圆形，直径8px）；在线时为淡粉色，显示“在线”文字（圆润字体，浅灰色，字号12px）；离线时为浅灰色，显示“离线”文字；状态实时同步，无需手动刷新。


**底部固定聊天输入栏**：
    样式：固定在页面底部（位于底部Tab栏上方），宽度全屏，高度56px，背景奶白色，淡粉色细边框（仅顶部有边框）；内部包含表情图标、输入框、发送按钮，左\-中\-右布局，表情图标和发送按钮各占据10%宽度，输入框占据80%宽度。


表情图标：淡粉色表情图标（尺寸24px×24px），位于输入栏左侧，点击后触发表情面板弹出操作，同时触发全局按压特效；表情面板为横向可滑动容器，背景奶白色，淡粉色细边框，大圆角12px，高度80px，位于输入栏上方，包含常用软萌表情（贴合宠物主题，如小狗、爱心、笑脸等），点击表情可快速输入到输入框，再次点击表情图标，关闭表情面板。


输入框：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，占位文字“发送消息…”（圆润字体，浅灰色，字号14px），输入文字为深灰色，圆润字体，字号14px，行间距1\.5倍，支持换行，无字数限制；输入时，输入框自动聚焦，弹出微信自带键盘，键盘样式贴合软萌风格。


发送按钮：主按钮样式，大圆角12px，高度44px，背景淡粉色，白色圆润加粗字体，字号14px，文字“发送”，位于输入栏右侧；输入框为空且无表情时，按钮置灰，不可点击；输入文字或添加表情后，按钮恢复正常状态，可点击。


交互逻辑：
    发送文字消息：输入文字后，点击“发送”按钮，触发发送操作，同时触发全局按压特效；发送成功后，输入框清空，聊天区域实时刷新，新消息显示在底部，自动滚动至最新消息；发送失败时，弹出提示弹窗（淡粉色叉号图标，标题“发送失败”，内容“消息发送失败，请检查网络后重试”，底部“重试”按钮），点击“重试”重新发送。


发送表情消息：点击表情图标，选择表情后，表情自动输入到输入框，点击“发送”按钮，触发发送操作，交互逻辑与文字消息一致；表情消息显示为文字形式（如“\[笑脸\]”），贴合轻量化定位。


发送图片消息：长按输入框，弹出“拍摄”“从相册选择”两个选项（复用全局选择弹窗样式），点击“拍摄”，调用微信相机拍摄图片，拍摄完成后自动发送；点击“从相册选择”，调用微信相册选择图片，选择完成后自动发送；图片发送时，聊天区域显示小狗转圈加载动画，加载完成后显示图片，发送失败时弹出提示弹窗，支持重试。


键盘收起：点击输入框以外的区域（聊天区域、表情面板），收起微信键盘；点击键盘上的“收起”按钮，也可收起键盘，输入框保持聚焦状态。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“消息”，交互逻辑符合全局规范；聊天页面底部Tab栏正常显示，不隐藏；聊天过程中，点击其他Tab项可切换页面，返回聊天页面时，自动定位到最新一条消息。


3\. 空状态与加载状态
    **空状态**：无任何聊天消息时，聊天区域居中显示可爱小狗对话插画（尺寸120px×120px），下方显示文字“还没有聊天记录哦～快来打个招呼吧🐶”（圆润字体，淡粉色，字号14px），文字居中；底部聊天输入栏正常显示，可正常发送消息。


**加载状态**：
    页面初始化加载：进入聊天页后，页面居中显示小狗转圈加载动画，下方显示“加载中…”文字，加载完成后，动画消失，显示聊天消息（或空状态）；加载失败时，弹出提示弹窗（提示“聊天记录加载失败，请重试”），点击“重试”重新加载页面。


历史消息加载：滚动聊天区域至顶部，自动触发历史消息加载操作，显示小狗转圈加载动画，加载完成后，动画消失，显示历史消息；若已加载全部历史消息，顶部显示“已加载全部历史消息”文字。

消息发送加载：发送图片消息时，消息位置显示小狗转圈加载动画，加载完成后显示图片，动画消失；发送文字/表情消息时，无加载动画，即时显示。


补充规则（贴合小程序聊天规范，提升用户体验）：
    陌生人聊天限制：未关注的陌生人之间，仅可发送1条私信消息，发送后，发送方输入框置灰，弹出提示弹窗（“陌生人仅可发送1条私信，对方回复后可正常聊天”）；若对方回复，解除限制，双方可正常聊天；若对方未回复，发送方无法继续发送消息，仅可查看历史消息。


消息提醒：聊天过程中，若当前页面不是该聊天页，收到新消息时，消息主页面的该私信卡片显示未读标记，同时小程序图标右上角添加红色数字（未读消息数量）；进入聊天页后，未读标记自动消失，小程序图标右上角数字同步更新。


聊天记录保存：聊天记录默认保存1年，超过1年的历史消息自动清理；清理前，弹出提示弹窗（“部分历史聊天记录即将过期，是否导出保存？”），点击“导出”，可将聊天记录导出为图片（长截图），点击“取消”，到期自动清理，清理后不可恢复。


## 3\.4 我的模块（核心页面，页面路径：/pages/mine/index）

### 3\.4\.1 页面结构（从上到下，完整无省略）

1. 顶部导航栏组件（复用全局组件）
    左侧：无图标（无操作）

2. 中间：标题“我的”


3. 右侧：设置图标（淡粉色，尺寸24px×24px，点击跳转设置页面）


4. 个人信息卡片（页面核心，展示用户及宠物核心信息）


5. 功能入口区域（纵向排列，6个核心功能入口，固定）


6. 底部辅助区域（关于我们、意见反馈、版本信息）


7. 底部Tab栏组件（复用全局组件）


我的模块作为用户个人中心，聚焦“个人信息管理、功能快捷入口、系统设置”三大核心，所有样式严格遵循全局软萌风格，交互逻辑与前文统一，确保用户能快速管理个人信息、便捷访问核心功能，以下详细说明各区域样式、交互逻辑及相关子页面规范，无任何细节省略。

### 3\.4\.2 各区域详细样式与交互逻辑（无省略）

#### 3\.4\.2\.1 顶部导航栏（复用全局组件，适配个人中心场景）

- 样式：严格复用全局顶部导航栏组件，宽度全屏、高度48px，背景奶白色（\#FFF9F9），底部淡粉色（\#FFC1E9）细边框，布局为“左\-中\-右”三栏，左右留16px安全边距。


- 左侧区域：无任何图标，不占位，保持页面简洁，贴合个人中心视觉风格。


- 中间区域：标题“我的”，圆润加粗字体，字号18px，颜色淡粉色（\#FFC1E9），居中显示，字数2字，符合全局标题规范。


- 右侧区域：淡粉色（\#FFC1E9）设置图标（齿轮样式，软萌卡通风格，尺寸24px×24px），点击后触发跳转操作，同时触发全局按压特效，平滑跳转至设置页面；图标始终固定在右侧，不随页面滚动消失。


- 交互逻辑：导航栏始终固定在页面顶部，不随页面滚动消失；仅右侧设置图标可点击，其他区域无交互操作，仅作标题展示和视觉引导。


#### 3\.4\.2\.2 个人信息卡片（核心展示区域，用户第一眼可见）

- 基础样式：大圆角12px卡片，宽度为屏幕宽度的95%，居中显示，高度180px，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，轻微阴影（rgba\(255, 193, 233, 0\.2\)），与顶部导航栏下方间距20px，符合全局布局间距规范。


- 内部布局（左\-右布局，清晰分区）：
    左侧：用户及宠物头像区域，占据卡片宽度的30%，居中显示；包含两个叠加头像（用户头像在上，宠物头像在下，轻微错位），均为圆形，用户头像直径60px，宠物头像直径40px，均添加淡粉色细边框，背景奶白色；用户未设置头像时，显示默认可爱小狗头像（用户默认头像），宠物未设置头像时，显示默认宠物头像（对应品种图标）。


- 右侧：用户及宠物信息区域，占据卡片宽度的70%，内边距左右16px，布局从上到下，间距8px：
    用户昵称：圆润加粗字体，淡粉色（\#FFC1E9），字号18px，居中对齐左侧头像；若昵称过长（超过8字），显示前6字\+省略号。


- 宠物信息：圆润字体，深灰色（\#333333），字号14px，显示“宠物名称 · 品种 · 年龄”（如“糯米 · 金毛 · 2岁”）；若未添加宠物信息，显示“点击添加宠物信息”（淡粉色文字，可点击）。


- 个性签名：圆润字体，浅灰色（\#E5E5E5），字号12px，可换行，最多显示2行，超出部分显示省略号；若用户无个性签名，显示“暂无个性签名”。


- 编辑按钮：大圆角8px按钮，高度32px，宽度80px，背景淡粉色，白色圆润加粗字体，字号14px，文字“编辑”，位于信息区域底部右侧；点击后触发跳转操作，同时触发全局按压特效，平滑跳转至个人信息编辑页。


交互逻辑：
    卡片点击：点击个人信息卡片任意区域（除编辑按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至自己的用户主页详情页（与他人用户主页样式一致，仅多编辑入口）。


头像点击：点击用户头像或宠物头像，触发头像预览操作，同时触发全局按压特效；预览时，头像全屏显示，支持左右滑动切换（用户头像、宠物头像），点击屏幕任意区域关闭预览；预览时，右上角显示“更换头像”按钮（主按钮样式，小尺寸），点击可弹出选择弹窗（“拍照”“从相册选择”），更换对应头像，交互逻辑与发布动态媒体上传一致。


宠物信息点击：若未添加宠物信息，点击“点击添加宠物信息”文字，触发跳转操作，同时触发全局按压特效，平滑跳转至宠物信息编辑页；已添加宠物信息时，点击无额外操作，仅作查看。


编辑按钮点击：点击“编辑”按钮，跳转至个人信息编辑页，可修改用户昵称、个性签名、宠物信息等，编辑完成后返回，个人信息卡片实时刷新，显示最新内容。


#### 3\.4\.2\.3 功能入口区域（核心功能快捷访问，固定6个入口）

- 基础样式：采用纵向网格布局，2列3行，宽度为屏幕宽度的95%，居中显示，与个人信息卡片下方间距20px；每个功能入口为独立的大圆角12px卡片，背景奶白色（\#FFF9F9），淡粉色（\#FFC1E9）细边框，轻微阴影，高度80px，两列入口间距12px，两行入口间距12px，符合全局卡片规范。


- 入口统一结构：每个入口内部布局从上到下，图标\+文字，居中显示；图标为淡粉色（\#FFC1E9）软萌卡通样式，尺寸28px×28px；文字为圆润字体，淡粉色，字号14px，位于图标下方，间距8px，文字与图标居中对齐。


- 6个功能入口（固定，不可修改，顺序不可调整，适配宠物社交场景）：
    1\. 我的关注：图标为淡粉色“关注”图标（爱心样式），文字“我的关注”；点击后跳转至我的关注列表页，查看所有已关注的用户。


- 2\. 我的粉丝：图标为淡粉色“粉丝”图标（用户群样式），文字“我的粉丝”；点击后跳转至我的粉丝列表页，查看所有关注自己的用户。


- 3\. 我的动态：图标为淡粉色“动态”图标（相机样式），文字“我的动态”；点击后跳转至我的动态列表页，查看自己发布的所有动态。


- 4\. 我的收藏：图标为淡粉色“收藏”图标（星星样式），文字“我的收藏”；点击后跳转至我的收藏列表页，查看所有收藏的动态、地点、门店。

- 5\. 宠物信息：图标为淡粉色“宠物”图标（小狗样式），文字“宠物信息”；点击后跳转至宠物信息编辑页，查看、修改宠物的详细信息（品种、年龄、体型等）。


- 6\. 隐私设置：图标为淡粉色“隐私”图标（锁样式），文字“隐私设置”；点击后跳转至隐私设置页，设置隐私可见模式（公开/隐藏）、消息提醒等。


交互逻辑（所有功能入口通用）：
    入口点击：点击任意功能入口卡片，触发跳转操作，同时触发全局按压特效，平滑跳转至对应功能页面；跳转特效符合全局规范，页面加载时显示小狗转圈加载动画。


入口状态提示：若某功能入口无对应数据（如我的关注为空、我的动态为空），入口右下角添加淡灰色“空”字标记（圆润字体，字号12px），提醒用户无相关内容；点击该入口，进入对应页面后显示空状态，与前文空状态样式一致。


入口长按：长按任意功能入口，无任何操作（避免误触），不触发弹窗或跳转，保持交互简洁。


#### 3\.4\.2\.4 底部辅助区域（辅助功能，提升用户体验）

- 基础样式：宽度为屏幕宽度的95%，居中显示，与功能入口区域下方间距20px，与底部Tab栏上方间距20px；采用横向排列，3个辅助入口均匀分布，每个入口为文字样式，无背景、无边框，仅通过文字区分。


- 辅助入口样式：文字为淡粉色（\#FFC1E9），圆润字体，字号14px，居中显示；每个入口之间间距24px，整体居中对齐。


- 3个辅助入口（固定，不可修改）：
    1\. 关于我们：点击后弹出提示弹窗（复用全局提示弹窗，标题“关于我们”，内容“本小程序专注于同城宠物社交，为养狗用户提供互动、交流、导航服务，感谢您的使用～”，底部“确定”按钮），点击“确定”关闭弹窗。


- 2\. 意见反馈：点击后跳转至意见反馈页，可输入反馈内容、上传图片，提交反馈意见。


- 3\. 版本信息：点击后弹出提示弹窗（复用全局提示弹窗，标题“版本信息”，内容“当前版本：V1\.0\.0，已是最新版本”，底部“确定”按钮），点击“确定”关闭弹窗；若有新版本，内容显示“当前版本：V1\.0\.0，发现新版本V1\.0\.1，是否更新？”，底部双按钮“取消”“立即更新”，点击“立即更新”，跳转至微信小程序更新页面。


#### 3\.4\.2\.5 底部Tab栏（复用全局组件，适配个人中心场景）

- 样式：严格复用全局底部Tab栏组件，宽度全屏、高度56px，背景奶白色（\#FFF9F9），顶部淡粉色（\#FFC1E9）细边框，4个Tab项均匀分布，样式、尺寸、图标完全符合全局规范。


- 选中状态：进入我的页面后，底部Tab栏“我的”项自动切换为选中状态（浅粉色用户头像图标，轻微放大至1\.1倍；文字淡粉色、加粗，字号12px），其他Tab项（首页、萌宠圈、消息）为未选中状态，与全局Tab栏选中规则一致。


- 交互逻辑：点击其他Tab项（首页、萌宠圈、消息），触发页面切换操作，同时触发全局按压特效，切换至对应页面；重复点击“我的”Tab项，无任何操作（避免重复跳转）；Tab栏始终固定在页面底部，不随页面滚动消失，保持视觉统一性。


### 3\.4\.3 空状态与加载状态（无省略，贴合个人中心场景）

- **空状态（分两类场景，样式统一，贴合软萌风格）**：
    场景1：个人信息未完善（未设置昵称、未添加宠物信息）：个人信息卡片显示默认头像，昵称显示“未设置昵称”，宠物信息显示“点击添加宠物信息”，个性签名显示“暂无个性签名”；无其他空状态提示，仅通过文字引导用户完善信息。


- 场景2：功能入口无数据（如我的关注为空、我的动态为空）：对应功能入口右下角显示淡灰色“空”字标记，进入对应功能页面后，显示对应空状态（如我的关注为空，显示小狗关注插画\+“暂无关注的用户哦～快去关注有趣的狗友吧”文字），与前文空状态样式、字体、颜色一致。


- **加载状态（分两类场景，复用全局加载特效）**：
    页面初始化加载：进入我的页面后，页面居中显示小狗转圈加载动画（淡粉色，尺寸40px×40px），下方显示“加载中…”文字（圆润字体，淡粉色，字号14px），加载完成后，动画消失，显示个人信息卡片、功能入口区域等内容；若加载失败，弹出提示弹窗（淡粉色叉号图标，标题“加载失败”，内容“个人中心加载失败，请重试”，底部“重试”按钮），点击“重试”按钮，重新加载页面，同时触发全局按压特效。


- 功能入口跳转加载：点击任意功能入口，跳转至对应页面时，页面居中显示小狗转圈加载动画，加载完成后，动画消失，显示对应页面内容（或空状态）；加载失败时，弹出相同的加载失败提示弹窗，支持重试。


### 3\.4\.4 我的模块子页面（共7个，无遗漏，含所有样式、交互）

#### 子页面1：个人信息编辑页（页面路径：/pages/mine/editInfo）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“编辑个人信息”，右侧“保存”按钮（主按钮样式，大圆角8px，高度32px，宽度80px，背景淡粉色，白色文字，字号14px）。

- 主体内容：编辑区域（用户头像、宠物头像、用户昵称、个性签名、宠物信息）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到我的主页面），同时触发全局按压特效；若有未保存的编辑内容，返回前弹出确认弹窗（复用全局确认弹窗，提示“确定要放弃编辑吗？未保存内容将丢失”，底部双按钮“取消”“确定”），点击“确定”放弃编辑并返回，点击“取消”留在当前页面；中间标题“编辑个人信息”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧“保存”按钮点击后，触发保存操作，同时触发全局按压特效，保存成功后弹出提示弹窗，自动返回我的主页面，个人信息卡片实时刷新；保存失败时，弹出提示弹窗，支持重试。


**主体内容：编辑区域**采用纵向布局，每个编辑项为独立条目，宽度为屏幕宽度的95%，居中显示，条目之间间距16px，与导航栏下方间距20px；每个编辑项包含“标签\+编辑内容”，左\-右布局，标签宽度20%，编辑内容宽度80%。


编辑项详细样式与交互（按顺序排列）：
    1\. 用户头像编辑：
        标签：“我的头像”，圆润字体，淡粉色，字号14px，居左显示；
        编辑内容：圆形头像（直径60px，淡粉色细边框），显示当前用户头像，右侧添加“更换”文字（淡粉色，圆润字体，字号14px）；点击头像或“更换”文字，弹出选择弹窗（“拍照”“从相册选择”），更换头像，交互逻辑与前文一致；头像上传时显示小狗转圈加载动画，加载完成后实时预览。


2\. 宠物头像编辑：
        标签：“宠物头像”，圆润字体，淡粉色，字号14px，居左显示；
        编辑内容：圆形头像（直径40px，淡粉色细边框），显示当前宠物头像，右侧添加“更换”文字；交互逻辑与用户头像编辑一致，支持拍照、从相册选择更换。


3\. 用户昵称编辑：
        标签：“我的昵称”，圆润字体，淡粉色，字号14px，居左显示；
        编辑内容：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前昵称，占位文字“请输入昵称（1\-8字）”；输入限制：1\-8字，支持中文、英文、数字，不可输入特殊字符；输入时实时校验，超出字数显示红色提示文字（“昵称最多8字”）。


4\. 个性签名编辑：
        标签：“个性签名”，圆润字体，淡粉色，字号14px，居左显示；
        编辑内容：复用全局输入框组件，高度自适应（默认44px，可换行），大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前个性签名，占位文字“请输入个性签名（最多50字）”；输入限制：最多50字，支持任意字符，输入时实时显示剩余字数（淡灰色，字号12px，位于输入框右下角）。


5\. 宠物信息编辑：
        标签：“宠物信息”，圆润字体，淡粉色，字号14px，居左显示；
        编辑内容：包含3个小输入框（宠物名称、品种、年龄），横向排列，均匀分布，每个输入框复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框；占位文字分别为“宠物名称”“品种”“年龄（如2岁）”；输入限制：宠物名称1\-6字，品种1\-8字，年龄为数字\+单位（如2岁、3个月），不可输入特殊字符；输入时实时校验，不符合规则显示红色提示文字。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“我的”，交互逻辑符合全局规范；编辑页面底部Tab栏正常显示，不隐藏。


3\. 加载状态
    头像上传加载：更换头像时，头像位置显示小狗转圈加载动画，加载完成后显示新头像，动画消失；加载失败时，弹出提示弹窗，支持重新上传。


保存加载：点击“保存”按钮后，页面居中显示小狗转圈加载动画，下方显示“保存中…”文字，保存完成后，动画消失，弹出提示弹窗，自动返回我的主页面；保存失败时，动画消失，弹出提示弹窗，支持重试。


#### 子页面2：我的关注列表页（页面路径：/pages/mine/follow）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“我的关注”，右侧无图标。


- 主体内容：关注用户列表区域（按关注时间由新到旧排序）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到我的主页面），同时触发全局按压特效；中间标题“我的关注”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧无图标，不占位；导航栏始终固定在页面顶部，不随页面滚动消失。


**主体内容：关注用户列表区域**采用纵向卡片列表布局，每个关注用户为一张独立的通用卡片组件，卡片宽度为屏幕宽度的95%，居中显示，卡片之间间距12px，与导航栏下方间距20px，与底部Tab栏上方间距20px；卡片样式与同片区热门狗友列表页的用户卡片完全一致（包含宠物头像、用户昵称、宠物信息、个性签名、操作按钮），仅操作按钮有差异。


卡片操作按钮差异：
    操作按钮为单个“取消关注”按钮（主按钮样式，背景淡粉色，白色文字，字号14px），位于卡片底部右侧，宽度80px；点击按钮，触发取消关注操作，同时触发全局按压特效，弹出确认弹窗（“确定要取消关注该用户吗？”），点击“确定”取消关注，卡片实时从列表中删除，页面同步刷新；点击“取消”，关闭确认弹窗，不执行取消操作。


交互逻辑：
    卡片点击：点击用户卡片任意区域（除取消关注按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至该用户的他人用户主页详情页。


列表排序：默认按“关注时间由新到旧”排序，最新关注的用户显示在顶部，排序规则不可修改。


下拉刷新与上拉加载：与前文列表逻辑一致，下拉刷新加载最新关注列表，上拉加载更多关注用户；若已加载全部关注用户，底部显示“已加载全部关注”文字，居中显示。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“我的”，交互逻辑符合全局规范；列表页面底部Tab栏正常显示，不隐藏。


3\. 空状态与加载状态**空状态**：无任何关注用户时，列表区域居中显示可爱小狗关注插画（尺寸120px×120px），下方显示文字“暂无关注的用户哦～快去关注有趣的狗友吧🐶”（圆润字体，淡粉色，字号14px），文字居中；顶部导航栏、底部Tab栏正常显示；点击文字或插画，可触发跳转操作，平滑跳转至同片区热门狗友列表页。


**加载状态**：页面初始化加载、下拉刷新、上拉加载时，显示全局统一的小狗转圈加载动画，加载失败时弹出提示弹窗，支持重试，与前文加载规范一致。


#### 子页面3：我的粉丝列表页（页面路径：/pages/mine/fans）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“我的粉丝”，右侧无图标。

- 主体内容：粉丝用户列表区域（按关注时间由新到旧排序）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）**顶部导航栏**：与我的关注列表页导航栏完全一致，左侧返回图标点击返回我的主页面，中间标题“我的粉丝”居中显示，右侧无图标，交互逻辑一致。


**主体内容：粉丝用户列表区域**卡片样式与我的关注列表页完全一致（复用通用卡片组件），仅操作按钮有差异；列表排序默认按“粉丝关注时间由新到旧”排序，最新关注的粉丝显示在顶部，排序规则不可修改。

卡片操作按钮差异：
    操作按钮为单个“关注”按钮，样式分两种状态：未关注该粉丝时，为次要按钮样式（背景浅灰色，深灰色文字）；已关注该粉丝时，为主按钮样式（背景淡粉色，白色文字，文字“已关注”）；点击按钮，触发关注/取消关注操作，同时触发全局按压特效，按钮状态实时切换，页面同步刷新；取消关注时，无需弹出确认弹窗，直接取消。


交互逻辑：
    卡片点击：点击用户卡片任意区域（除关注按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至该粉丝的他人用户主页详情页。


下拉刷新与上拉加载：与我的关注列表页逻辑一致，下拉刷新加载最新粉丝列表，上拉加载更多粉丝；已加载全部粉丝时，底部显示“已加载全部粉丝”文字。


**底部Tab栏**：与我的关注列表页一致，选中状态保持为“我的”，正常显示，不隐藏。


3\. 空状态与加载状态
    **空状态**：无任何粉丝时，列表区域居中显示可爱小狗粉丝插画（尺寸120px×120px），下方显示文字“暂无粉丝哦～发布动态吸引更多狗友关注吧🐶”（圆润字体，淡粉色，字号14px），文字居中；点击文字或插画，可触发跳转操作，平滑跳转至发布动态页。


**加载状态**：与我的关注列表页一致，加载动画、失败提示规范统一。


#### 子页面4：我的动态列表页（页面路径：/pages/mine/myDynamic）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“我的动态”，右侧“发布”按钮（主按钮样式，大圆角8px，高度32px，宽度80px，背景淡粉色，白色文字，字号14px）。


- 主体内容：自己发布的动态列表区域（按发布时间由新到旧排序）。


- 底部Tab栏组件（复用全局组件）。

2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到我的主页面），同时触发全局按压特效；中间标题“我的动态”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧“发布”按钮点击后，触发跳转操作，同时触发全局按压特效，平滑跳转至发布动态页；导航栏始终固定在页面顶部，不随页面滚动消失。


**主体内容：我的动态列表区域**动态卡片样式与萌宠圈主页面的动态卡片完全一致（包含发布者信息、动态内容、媒体区域、互动栏），仅互动栏有差异（无分享按钮，多删除、编辑按钮）；列表排序默认按“发布时间由新到旧”排序，最新发布的动态显示在顶部，排序规则不可修改。


动态卡片差异：
    互动栏右侧添加“编辑”“删除”两个按钮（淡粉色图标，尺寸20px×20px），与点赞、评论按钮并排，间距8px；编辑按钮为“编辑”图标，删除按钮为“删除”图标，样式统一为淡粉色软萌风格。


交互逻辑：
    卡片点击：点击动态卡片任意区域（除编辑、删除按钮外），触发跳转操作，同时触发全局按压特效，平滑跳转至该动态的详情页。


编辑按钮：点击编辑按钮，触发跳转操作，同时触发全局按压特效，平滑跳转至发布动态编辑页，编辑页自动填充该动态的原有内容（文字、图片/视频），编辑完成后保存，动态列表实时刷新，显示最新内容。


删除按钮：点击删除按钮，触发删除确认操作，同时触发全局按压特效，弹出全局确认弹窗（“确定要删除这条动态吗？删除后不可恢复”），点击“确定”，删除该动态，列表实时刷新，删除成功后弹出提示弹窗；点击“取消”，关闭确认弹窗，不执行删除操作。

点赞、评论交互：与萌宠圈主页面一致，点击点赞按钮切换点赞状态，点击评论按钮弹出评论弹窗，交互逻辑完全统一。


下拉刷新与上拉加载：与萌宠圈主页面列表逻辑一致，下拉刷新加载最新动态，上拉加载更多历史动态；已加载全部动态时，底部显示“已加载全部动态”文字。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“我的”，交互逻辑符合全局规范；列表页面底部Tab栏正常显示，不隐藏。


3\. 空状态与加载状态
    **空状态**：未发布任何动态时，列表区域居中显示可爱小狗相机插画（尺寸120px×120px），下方显示文字“暂无动态哦～发布你的萌宠日常吧🐶”（圆润字体，淡粉色，字号14px），文字居中；点击文字或插画，可触发跳转操作，平滑跳转至发布动态页。


**加载状态**：页面初始化加载、下拉刷新、上拉加载时，显示全局统一的小狗转圈加载动画，加载失败时弹出提示弹窗，支持重试，与萌宠圈加载规范一致。


#### 子页面5：我的收藏列表页（页面路径：/pages/mine/collection）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“我的收藏”，右侧无图标。


- 分类筛选栏（横向可滑动，3个分类，固定）：位于导航栏下方，间距16px。


- 主体内容：收藏列表区域（按收藏时间由新到旧排序）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：与我的关注列表页导航栏完全一致，左侧返回图标点击返回我的主页面，中间标题“我的收藏”居中显示，右侧无图标，交互逻辑一致。


**分类筛选栏**：与附近遛狗好去处列表页的分类筛选栏样式完全一致，横向可滑动容器，高度44px，背景奶白色，淡粉色细边框，大圆角12px，宽度为屏幕宽度的95%，居中显示；包含3个分类标签，均匀分布，标签样式分选中/未选中状态，与全局选中样式一致。


分类标签内容（固定3个，不可修改）：
    全部收藏：默认选中，显示所有类型的收藏内容（动态、地点、门店）；
    动态收藏：仅显示收藏的动态；
    地点门店收藏：仅显示收藏的宠物友好地点、宠物门店。


交互逻辑：点击任意分类标签，触发分类切换操作，同时触发全局按压特效，列表区域实时刷新，显示对应分类的收藏内容，刷新时显示小狗转圈加载动画。

**主体内容：收藏列表区域**根据分类不同，显示不同类型的收藏卡片，所有卡片均复用对应页面的卡片样式，保持风格统一：
    1\. 动态收藏卡片：与萌宠圈主页面的动态卡片完全一致，交互逻辑一致（点击跳转详情页、取消收藏等）；卡片右上角添加淡粉色“取消收藏”图标（尺寸20px×20px），点击图标，触发取消收藏操作，同时触发全局按压特效，弹出提示弹窗，卡片实时从列表中删除，页面同步刷新。

2\. 地点/门店收藏卡片：与附近遛狗好去处列表页的地点/门店卡片完全一致，交互逻辑一致（点击跳转详情页、导航等）；卡片右上角添加淡粉色“取消收藏”图标，点击图标，触发取消收藏操作，交互逻辑与动态收藏取消一致。


列表排序：默认按“收藏时间由新到旧”排序，最新收藏的内容显示在顶部，排序规则不可修改。


下拉刷新与上拉加载：与前文列表逻辑一致，下拉刷新加载最新收藏列表，上拉加载更多收藏内容；已加载全部收藏时，底部显示“已加载全部收藏”文字。


**底部Tab栏**：与我的关注列表页一致，选中状态保持为“我的”，正常显示，不隐藏。


3\. 空状态与加载状态
    **空状态**：无任何收藏内容（或某分类下无收藏）时，列表区域居中显示可爱小狗星星插画（尺寸120px×120px），下方显示文字“暂无收藏哦～快去收藏喜欢的内容吧🐶”（圆润字体，淡粉色，字号14px），文字居中；点击文字或插画，可触发跳转操作，平滑跳转至萌宠圈主页面或附近遛狗好去处列表页。


**加载状态**：页面初始化加载、分类切换加载、下拉刷新、上拉加载时，显示全局统一的小狗转圈加载动画，加载失败时弹出提示弹窗，支持重试，与前文加载规范一致。


#### 子页面6：宠物信息编辑页（页面路径：/pages/mine/petInfo）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“编辑宠物信息”，右侧“保存”按钮（主按钮样式，大圆角8px，高度32px，宽度80px，背景淡粉色，白色文字，字号14px）。


- 主体内容：宠物信息编辑区域（宠物头像、宠物名称、品种、年龄、体型、性别、个性签名）。

- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：与个人信息编辑页导航栏完全一致，左侧返回图标点击返回我的主页面，未保存内容时弹出确认弹窗；中间标题“编辑宠物信息”居中显示，右侧“保存”按钮点击后保存编辑内容，保存成功/失败提示与个人信息编辑一致。

**主体内容：宠物信息编辑区域**采用纵向布局，每个编辑项为独立条目，宽度为屏幕宽度的95%，居中显示，条目之间间距16px，与导航栏下方间距20px；每个编辑项包含“标签\+编辑内容”，左\-右布局，标签宽度20%，编辑内容宽度80%，样式与个人信息编辑页一致。


编辑项详细样式与交互（按顺序排列）：
    1\. 宠物头像编辑：与个人信息编辑页的宠物头像编辑完全一致，支持拍照、从相册选择更换，上传时显示加载动画。

2\. 宠物名称编辑：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前宠物名称，占位文字“请输入宠物名称（1\-6字）”；输入限制：1\-6字，支持中文、英文、数字，不可输入特殊字符。


3\. 宠物品种编辑：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前宠物品种，占位文字“请输入宠物品种（1\-8字）”；输入限制：1\-8字，支持中文、英文，不可输入特殊字符；输入时实时联想，显示常见品种选项（如金毛、柯基），点击可快速选择。


4\. 宠物年龄编辑：复用全局输入框组件，高度44px，大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前宠物年龄，占位文字“请输入年龄（如2岁、3个月）”；输入限制：数字\+单位，不可输入特殊字符，实时校验，不符合规则显示红色提示文字。


5\. 宠物体型选择：复用全局选择弹窗组件，点击编辑内容区域，弹出选择弹窗，选项为“小型犬”“中型犬”“大型犬”，默认选中当前体型；选中后，编辑内容区域显示选中的体型文字（淡粉色，圆润字体，字号14px），同时触发全局按压特效。


6\. 宠物性别选择：复用全局选择弹窗组件，点击编辑内容区域，弹出选择弹窗，选项为“公”“母”，默认选中当前性别；选中后，编辑内容区域显示选中的性别文字（淡粉色，圆润字体，字号14px），同时触发全局按压特效。

7\. 宠物个性签名编辑：复用全局输入框组件，高度自适应（默认44px，可换行），大圆角12px，背景奶白色，淡粉色细边框，输入框内显示当前宠物个性签名，占位文字“请输入宠物个性签名（最多30字）”；输入限制：最多30字，支持任意字符，实时显示剩余字数。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“我的”，交互逻辑符合全局规范；编辑页面底部Tab栏正常显示，不隐藏。


3\. 加载状态
    头像上传加载：与个人信息编辑页一致，加载动画、失败提示规范统一。

保存加载：点击“保存”按钮后，页面居中显示小狗转圈加载动画，下方显示“保存中…”文字，保存完成后，动画消失，弹出提示弹窗，自动返回我的主页面，个人信息卡片的宠物信息实时刷新；保存失败时，动画消失，弹出提示弹窗，支持重试。

#### 子页面7：设置页面（页面路径：/pages/mine/setting）

- 1\. 页面结构（从上到下）
    顶部导航栏组件（复用全局组件）：左侧返回图标（点击返回我的主页面），中间标题“设置”，右侧无图标。

- 主体内容：设置选项区域（纵向排列，5个核心设置选项，固定）。


- 底部退出登录按钮（通栏主按钮）。


- 底部Tab栏组件（复用全局组件）。


2\. 各区域详细样式与交互逻辑（无省略）
    **顶部导航栏**：复用全局顶部导航栏组件，左侧返回图标点击后，触发页面返回操作（回到我的主页面），同时触发全局按压特效；中间标题“设置”居中显示，圆润加粗字体，淡粉色，字号18px，符合全局规范；右侧无图标，不占位；导航栏始终固定在页面顶部，不随页面滚动消失。

**主体内容：设置选项区域**采用纵向布局，每个设置选项为独立条目，宽度为屏幕宽度的95%，居中显示，条目之间间距8px，与导航栏下方间距20px；每个条目为左\-右布局，左侧为设置标签\+图标，右侧为开关/跳转箭头，样式统一。


设置选项样式（统一规范）：
    左侧：淡粉色软萌图标（尺寸24px×24px）\+ 设置标签（圆润字体，淡粉色，字号14px），图标与标签间距8px；
    右侧：根据选项类型显示开关或跳转箭头；开关为淡粉色样式（开启时背景淡粉色，关闭时背景浅灰色）；跳转箭头为淡粉色（尺寸20px×20px），指向右侧。

5个设置选项（固定，不可修改，顺序不可调整）：
    1\. 隐私可见模式：图标为淡粉色“锁”图标，标签“隐私可见模式”；右侧为开关，开启时（默认），用户点位显示在首页地图上，可被其他用户看到；关闭时，用户点位隐藏，仅自己可见；点击开关，触发切换操作，同时触发全局按压特效，开关状态实时切换，弹出提示弹窗（“隐私可见模式已开启/关闭”）。


2\. 消息提醒设置：图标为淡粉色“铃铛”图标，标签“消息提醒设置”；右侧为跳转箭头，点击后跳转至消息提醒设置子页，可设置各类消息（评论、回复、关注、私信）的提醒方式（开启/关闭）。


3\. 清除缓存：图标为淡粉色“清理”图标，标签“清除缓存”；右侧为文字“清理”（淡粉色，圆润字体，字号14px），点击后触发清除缓存操作，同时触发全局按压特效，弹出确认弹窗（“确定要清除缓存吗？清除后不影响用户数据”），点击“确定”，开始清除缓存，显示小狗转圈加载动画，清除完成后弹出提示弹窗（“缓存已清除”）；点击“取消”，关闭确认弹窗。


4\. 账号安全：图标为淡粉色“安全”图标，标签“账号安全”；右侧为跳转箭头，点击后跳转至账号安全页，可修改登录密码、绑定手机号等（贴合微信小程序规范，可复用微信登录授权）。


5\. 帮助中心：图标为淡粉色“帮助”图标，标签“帮助中心”；右侧为跳转箭头，点击后跳转至帮助中心页，包含常见问题（如如何发布动态、如何关注用户、如何取消收藏等）、联系客服入口。


**底部退出登录按钮**：
    样式：通栏主按钮，宽度为屏幕宽度的95%，高度44px，大圆角12px，背景淡粉色，白色圆润加粗字体，字号16px，文字“退出登录”，居中显示，与设置选项区域下方间距20px，与底部Tab栏上方间距20px。


交互逻辑：点击按钮，触发退出登录操作，同时触发全局按压特效，弹出确认弹窗（“确定要退出登录吗？退出后需重新登录”），点击“确定”，退出当前账号，跳转至小程序登录页面（微信授权登录）；点击“取消”，关闭确认弹窗，不执行退出操作。


**底部Tab栏**：复用全局底部Tab栏组件，选中状态保持为“我的”，交互逻辑符合全局规范；设置页面底部Tab栏正常显示，不隐藏。


3\. 加载状态
    清除缓存加载：点击“清除缓存”按钮，确认后显示小狗转圈加载动画，加载完成后动画消失，弹出提示弹窗；加载失败时，弹出提示弹窗（“缓存清除失败，请重试”），支持重试。


页面跳转加载：点击消息提醒设置、账号安全、帮助中心选项，跳转至对应子页时，显示小狗转圈加载动画，加载完成后动画消失，显示对应页面内容；加载失败时，弹出提示弹窗，支持重试。


# 四、异常场景处理规范（全场景覆盖，无遗漏）

## 4\.1 网络异常（最常见场景）

### 4\.1\.1 场景描述

用户使用小程序时，网络中断（无网络）、网络信号弱（加载缓慢）、网络不稳定（时断时续），导致页面加载失败、操作无法完成（如发布动态、发送消息、加载列表等）。

### 4\.1\.2 处理规范（统一样式、统一交互）

- 1\. 视觉提示：页面居中显示可爱小狗网络异常插画（尺寸120px×120px），插画下方显示文字“网络异常，请检查网络连接后重试🐶”（圆润字体，淡粉色，字号14px），文字居中；下方添加“重新连接”按钮（主按钮样式，大圆角12px，高度44px，背景淡粉色，白色文字，字号16px），与文字间距16px。


- 2\. 交互逻辑：
    点击“重新连接”按钮，触发重新加载操作，同时触发全局按压特效，显示小狗转圈加载动画；若网络恢复，加载完成后，动画消失，显示页面正常内容；若网络仍异常，加载失败后，保持网络异常提示，不重复弹窗。


3\. 补充规则：
    网络中断时，正在进行的操作（如发布动态、发送消息）自动暂停，不强制提交，待网络恢复后，弹出提示弹窗（“网络已恢复，是否继续操作？”），点击“继续”，恢复当前操作；点击“取消”，放弃当前操作，返回操作前状态。
    网络信号弱时，页面加载过程中显示加载动画，加载超时（超过10秒）后，显示网络异常提示，支持重新连接；加载过程中，用户可点击加载动画，触发重新加载操作。
    所有页面均需适配网络异常场景，无例外；网络异常提示不遮挡底部Tab栏，保持页面布局完整性。

### 4\.1\.3 特殊场景补充

- 发布动态/发送消息时网络异常：输入的内容自动本地缓存（缓存时间24小时），网络恢复后，弹窗提示“发现未提交内容，是否继续发布/发送？”，点击“继续”，自动提交缓存内容；点击“取消”，清除本地缓存，用户可重新输入。

- 图片/视频上传时网络异常：上传进度暂停，显示“网络异常，上传暂停”提示文字（淡粉色，圆润字体，字号12px）；网络恢复后，自动继续上传，进度实时更新；若上传失败（超过3次重试仍失败），弹出提示弹窗（“上传失败，请重新上传”），支持重新选择媒体文件上传。

## 4\.2 操作异常（用户操作不当或操作失败）

### 4\.2\.1 场景分类及处理规范

#### 场景1：输入内容不符合规则（如昵称过长、年龄格式错误等）

- 视觉提示：输入框下方显示红色提示文字（圆润字体，字号12px），明确提示错误原因（如“昵称最多8字”“年龄格式错误，请输入数字\+单位，如2岁”）；输入框边框变为淡红色，与正常状态区分。

- 交互逻辑：输入时实时校验，不符合规则时，对应操作按钮（如“保存”“发布”）置灰，不可点击；修改输入内容至符合规则后，红色提示文字消失，输入框边框恢复淡粉色，操作按钮恢复可点击状态；不弹出弹窗，仅通过 inline 提示，避免打扰用户。

#### 场景2：操作触发限制（如陌生人发送超过1条私信、频繁点赞/评论等）

- 视觉提示：弹出全局提示弹窗（淡粉色感叹号图标，标题“操作限制”，内容根据场景定制），底部单个“确定”按钮。

- 交互逻辑：
    陌生人私信限制：发送1条私信后，输入框置灰，弹出提示弹窗（“陌生人仅可发送1条私信，对方回复后可正常聊天”），点击“确定”关闭弹窗，输入框保持置灰，直至对方回复或关注对方。
    频繁操作限制：短时间内（1分钟内超过5次）点赞、评论、关注，触发限制，弹出提示弹窗（“操作过于频繁，请稍后再试”），点击“确定”关闭弹窗，对应操作按钮置灰，1分钟后自动恢复可点击状态。

#### 场景3：操作失败（如取消关注失败、删除动态失败等，非网络原因）

- 视觉提示：弹出全局提示弹窗（淡粉色叉号图标，标题“操作失败”，内容“操作失败，请稍后重试”），底部单个“重试”按钮。

- 交互逻辑：点击“重试”按钮，触发重新操作，同时触发全局按压特效；若重试3次仍失败，弹出提示弹窗（“操作多次失败，请联系客服反馈”），底部“联系客服”按钮（点击跳转至帮助中心的客服入口）；不强制用户重试，用户可点击弹窗空白区域关闭弹窗，放弃操作。

## 4\.3 数据异常（数据加载失败、数据不存在、数据错误）

### 4\.3\.1 场景分类及处理规范

#### 场景1：数据加载失败（非网络原因，如服务器异常）

- 视觉提示：页面居中显示可爱小狗数据异常插画（尺寸120px×120px），插画下方显示文字“数据加载失败，请稍后重试🐶”（圆润字体，淡粉色，字号14px），文字居中；下方添加“重新加载”按钮（主按钮样式），与文字间距16px。

- 交互逻辑：点击“重新加载”按钮，触发重新加载操作，同时触发全局按压特效；若多次加载失败，弹出提示弹窗（“服务器暂时异常，请稍后再试或联系客服反馈”），支持联系客服；底部Tab栏正常显示，用户可切换至其他页面，避免长时间停留。

#### 场景2：数据不存在（如搜索无结果、查看的动态/用户已删除等）

- 视觉提示：页面居中显示对应场景的空状态插画（如搜索无结果显示小狗搜索插画），插画下方显示场景化提示文字（如“未找到符合条件的内容哦～”“该动态已删除或不存在”），圆润字体，淡粉色，字号14px，文字居中。

- 交互逻辑：无弹窗提示，仅显示空状态；用户可点击插画或提示文字，跳转至对应推荐页面（如搜索无结果，点击文字跳转至萌宠圈主页面）；若为查看单个内容（如动态、用户）不存在，页面添加“返回上一页”按钮（主按钮样式），点击返回上一页，同时触发全局按压特效。

#### 场景3：数据错误（如宠物年龄显示异常、动态时间显示错误等）

- 视觉提示：错误数据位置显示“\-\-”占位符，替代错误内容，同时在占位符右侧添加淡粉色“感叹号”小图标（尺寸16px×16px）；鼠标点击（或长按）感叹号图标，弹出提示文字（“数据加载异常，已记录问题”）。

- 交互逻辑：不影响其他操作，用户可正常使用小程序；后台自动记录数据错误问题，无需用户手动反馈；数据修复后，页面自动刷新，显示正确数据，无需用户手动操作。

## 4\.4 权限异常（微信授权失败、权限未开启）

### 4\.4\.1 场景分类及处理规范

#### 场景1：微信授权失败（如用户拒绝授权、授权过期）

- 视觉提示：页面全屏显示授权提示（背景奶白色，淡粉色细边框，大圆角12px），包含可爱小狗授权插画（尺寸120px×120px）、提示文字（“请授权微信信息，以便正常使用小程序功能🐶”）、“去授权”按钮（主按钮样式），所有元素居中显示。

- 交互逻辑：用户未授权时，无法进入小程序任何功能页面，仅显示授权提示；点击“去授权”按钮，跳转至微信授权页面，用户授权成功后，自动跳转至小程序首页；用户拒绝授权时，弹出提示弹窗（“未授权微信信息，部分功能无法使用，是否重新授权？”），底部双按钮“取消”“重新授权”，点击“重新授权”再次跳转至微信授权页面，点击“取消”，仍显示授权提示页面。

#### 场景2：权限未开启（如相机权限、相册权限、定位权限）

- 视觉提示：触发对应操作（如更换头像、发布动态上传图片、首页定位）时，弹出全局提示弹窗（淡粉色感叹号图标，标题“权限未开启”，内容根据场景定制，如“请开启相机权限，以便拍摄图片”），底部双按钮“取消”“去开启”。

- 交互逻辑：点击“去开启”按钮，跳转至微信小程序权限设置页面，用户开启对应权限后，自动返回当前页面，恢复操作；点击“取消”按钮，关闭弹窗，放弃当前操作，不影响其他功能使用；同一操作多次触发权限未开启提示，仅首次弹出弹窗，后续可通过设置页面手动开启权限。

## 4\.5 特殊场景异常（弹窗叠加、页面崩溃、版本过低）

### 4\.5\.1 场景分类及处理规范

#### 场景1：弹窗叠加（异常情况下出现多个弹窗同时弹出）

- 处理逻辑：后台自动拦截叠加弹窗，仅显示最新弹出的弹窗；前一个弹窗自动关闭，不提示用户；弹窗关闭后，若有后续弹窗，按触发顺序依次显示，避免叠加遮挡，确保用户操作清晰。

- 补充规则：所有弹窗均设置层级，加载弹窗层级最高（优先级最高，不被其他弹窗遮挡），确认弹窗、提示弹窗层级次之，选择弹窗层级最低；确保加载过程中，其他弹窗无法弹出，避免干扰用户。

#### 场景2：页面崩溃（如闪退、卡死）

- 处理逻辑：页面崩溃后，自动重启小程序，重启后跳转至首页，弹出提示弹窗（“页面出现异常，已重新启动小程序”），底部“确定”按钮；后台自动记录崩溃日志，用于问题修复。

- 补充规则：若连续3次崩溃，弹出提示弹窗（“小程序频繁异常，请更新版本或联系客服反馈”），底部“更新版本”“联系客服”两个按钮，点击“更新版本”跳转至微信小程序更新页面，点击“联系客服”跳转至帮助中心客服入口。

#### 场景3：小程序版本过低（未更新至最新版本）

- 视觉提示：用户进入小程序时，弹出全局确认弹窗（淡粉色提示图标，标题“版本更新”，内容“发现新版本V1\.0\.1，更新后可获得更好的体验，是否立即更新？”），底部双按钮“取消”“立即更新”。

- 交互逻辑：点击“立即更新”按钮，跳转至微信小程序更新页面，更新完成后自动重启小程序，跳转至首页；点击“取消”按钮，关闭弹窗，可继续使用旧版本，但部分新功能无法使用；旧版本使用过程中，每天首次触发新功能（如AI攻略生成）时，再次弹出更新提示，直至用户更新版本。

## 4\.6 异常场景补充规则（全场景通用）

- 所有异常提示均需贴合软萌风格，插画、文字、按钮样式与全局规范保持一致，避免出现生硬的系统提示，提升用户体验。

- 异常处理过程中，尽量减少用户操作步骤，避免多次弹窗、多次跳转，确保操作便捷；如网络异常仅需点击“重新连接”，无需额外操作。

- 所有异常日志（网络异常、操作失败、页面崩溃等）均需后台自动记录，包含异常场景、触发时间、用户操作，便于后续问题排查和优化。

- 异常提示文字需简洁明了，避免专业术语，让用户快速理解异常原因和解决方法；如“网络异常，请检查网络连接后重试”，不使用“网络请求超时，HTTP状态码404”等专业表述。

- 所有异常场景处理均需适配手机不同尺寸，确保提示弹窗、插画、按钮在各种屏幕尺寸下正常显示，不出现遮挡、错位等问题。

# 五、全局通用规则（贯穿全小程序，无例外）

## 5\.1 视觉通用规则

- 1\. 色彩规范：全局主色调为淡粉色（\#FFC1E9），辅助色为奶白色（\#FFF9F9）、浅灰色（\#E5E5E5）、深灰色（\#333333），点缀色为浅奶黄（\#FFF4D2）、柔浅蓝（\#E0F7FF）；所有页面、组件均需遵循该色彩规范，不可新增其他颜色，确保视觉统一性。

- 2\. 字体规范：全局统一使用圆润字体（贴合软萌风格），标题字号18px（加粗），副标题/按钮文字字号16px（主按钮加粗），正文文字字号14px，辅助文字（提示、时间等）字号12px；颜色按场景区分：标题、重点文字为淡粉色，正文文字为深灰色，辅助文字为浅灰色，错误提示为淡红色。

- 3\. 圆角规范：全局所有组件（卡片、按钮、输入框、弹窗等）均使用大圆角（12px），小标签、小按钮使用小圆角（8px），无直角组件，贴合软萌风格；圆角尺寸不可修改，确保所有组件风格统一。

- 4\. 图标规范：所有图标均为淡粉色软萌卡通样式，尺寸统一（导航栏图标24px×24px，Tab栏图标28px×28px，按钮图标20px×20px）；图标风格一致，均为简约卡通，贴合宠物主题（如小狗、爱心、相机等），不可使用写实、生硬的图标。

- 5\. 布局规范：所有页面均遵循“上下布局”，顶部为导航栏，中间为主体内容，底部为Tab栏；页面左右安全边距16px，上下间距20px，组件之间间距8px/12px/16px，确保布局整齐、不拥挤；主体内容可滚动，导航栏和Tab栏始终固定，不随页面滚动消失。

## 5\.2 交互通用规则

- 1\. 全局按压特效：所有可点击组件（按钮、卡片、图标、Tab项等），点击时均触发“微缩回弹\+震动\+粒子”特效，特效样式统一（微缩比例1\.05→0\.95→1\.0，震动时长0\.1秒，粒子为淡粉色小爱心/小狗爪印）；不可点击组件（置灰按钮、空状态等），无任何交互特效。

- 2\. 页面跳转规则：所有页面跳转均采用“平滑切换”特效（左右滑动/淡入淡出），跳转时显示小狗转圈加载动画；返回操作（点击返回图标）采用“反向平滑切换”特效，无加载动画；跳转过程中，不可触发其他操作，避免跳转异常。

- 3\. 加载动画规则：全局统一使用“小狗转圈”加载动画（淡粉色，尺寸40px×40px，循环播放）；加载场景包括：页面初始化、数据刷新、操作执行（如保存、删除、上传）；加载完成后，动画自动消失，不残留；加载超时（超过10秒），触发对应异常处理。

- 4\. 弹窗交互规则：所有弹窗弹出/关闭均采用“淡入淡出”特效，弹窗不可拖动、不可缩放；弹窗点击区域明确：按钮、图标可点击，空白区域可关闭（加载弹窗除外）；弹窗操作后，均有明确反馈（提示弹窗、页面刷新）。

- 5\. 列表交互规则：所有纵向列表均支持下拉刷新、上拉加载；下拉刷新显示顶部加载动画，上拉加载显示底部加载动画；列表项点击均触发跳转或操作，点击时触发全局按压特效；列表排序规则固定，不可修改，确保用户使用习惯统一。

## 5\.3 数据通用规则

- 1\. 数据加载规则：所有页面数据均采用“懒加载”模式，初始化时加载第一页数据，上拉加载下一页数据，减少页面加载时间；数据加载失败时，触发数据异常处理规范，支持重新加载。

- 2\. 数据缓存规则：用户输入内容（如编辑信息、发布动态草稿）、常用数据（如关注列表、收藏列表）自动本地缓存，缓存时间24小时；缓存数据优先显示，后台同步最新数据，同步完成后实时刷新页面，确保数据一致性。

- 3\. 数据排序规则：所有列表数据排序规则固定，不可修改：用户列表（距离由近到远/关注时间由新到旧）、动态列表（发布时间由新到旧）、收藏列表（收藏时间由新到旧），确保用户使用体验统一。

- 4\. 数据隐私规则：用户隐私数据（如精确位置、手机号）仅用于小程序对应功能（如定位、账号安全），不泄露、不收集无关隐私数据；用户可通过隐私设置关闭位置可见，关闭后，其他用户无法看到其点位信息。

## 5\.4 其他通用规则

- 1\. 适配规则：小程序适配所有主流手机尺寸（375px\-428px宽度），页面、组件自动适配，不出现遮挡、错位、拉伸等问题；字体、图标尺寸自适应，确保在不同屏幕尺寸下显示清晰、美观。

- 2\. 性能规则：页面加载时间不超过3秒，列表加载时间不超过2秒，图片/视频加载采用“缩略图\+原图”模式，提升加载速度；避免过多动画、过多组件叠加，防止页面卡顿、闪退。

- 3\. 一致性规则：所有相同类型的组件（如按钮、输入框、卡片），样式、交互完全统一；所有页面的导航栏、Tab栏样式完全统一；所有异常提示、加载动画完全统一，确保用户使用习惯统一，降低学习成本。

- 4\. 可访问性规则：文字对比度符合微信小程序可访问性规范（淡粉色文字与奶白色背景对比度≥4\.5:1），确保视力不佳用户可正常阅读；图标均有对应文字提示，避免纯图标无文字，提升可访问性。

- 5\. 版本更新规则：小程序版本更新后，自动弹出更新提示，用户更新后，保留原有用户数据（关注、收藏、动态等），不丢失；旧版本不强制更新，但部分新功能无法使用，引导用户更新至最新版本。

# 六、附则

## 6\.1 规范说明

本规范为同城宠物社交小程序（软萌风格）的全流程设计规范，涵盖页面设计、组件设计、功能模块、异常处理、全局规则等所有内容，适用于小程序所有页面、所有功能，所有开发、设计人员需严格遵循本规范，确保小程序视觉、交互、功能的统一性和一致性。

## 6\.2 规范更新

本规范将根据小程序迭代、用户反馈、功能优化等情况，适时更新完善；更新后，将发布新版本规范，明确更新内容，开发、设计人员需按新版本规范执行，旧版本规范自动失效。

## 6\.3 责任说明

开发、设计人员在开发、设计过程中，若未遵循本规范，导致小程序出现视觉不统一、交互异常、功能故障等问题，需及时整改；若因未遵循规范造成严重问题（如用户投诉、功能无法使用），将追究相关人员责任。

本规范自发布之日起生效，最终解释权归小程序开发团队所有。



> （注：文档部分内容可能由 AI 生成）
